# 支票頁 — 版面調整與開立進度存檔

修改日期：2026/07/25
異動檔案：`pages/check.html`、`css/check.css`、`js/check-engine.js`、`js/common-keypad.js`、`tests/`

**驗證結果：七支測試全過 — 380 項功能檢查、約 30 萬次鍵盤行為對照、82 萬筆大寫金額往返比對，零失敗。**

---

## 一、版面

### 大寫金額與張數說明的歸屬

`.amount-group` 原本沒有任何 CSS，形同不存在，所以大寫金額浮在框與框之間的空隙裡 —— 距離上下兩個框幾乎一樣遠，看不出它屬於誰。

現在補上與計算頁 `.input-button-group` 相同的外框，把輸入列與它的附屬說明綁成同一個視覺單位。不是新發明的樣式，是啟用一個本來就該存在的容器。

欄位空白時用 `:empty` 讓那一行完全收起，外框底部不會留空縫。

### 進度列改為固定於畫面頂端

從兩行（約 58px）壓成單行（約 38px），並固定在頂端，捲到第 40 列也還看得到。

同時把「現在時間」改為一般捲動。畫面上方只有一個固定位置，開票時「已開幾張」比「現在幾點」重要得多，兩條都固定會吃掉約 100px。這順帶解決了原本 `top: 65px` 的問題 —— 那個值是為計算頁的固定利率列留的，支票頁沒有那一列，捲動時上方會露出一條 65px 的空白。

---

## 二、漏開偵測

往下開票的過程中，下方本來就全是未打勾 —— 那不是漏開，是還沒開到。真正的漏開是**已打勾的列之中夾著未打勾的列**。

偵測方式：數「位置在最後一個打勾之上、卻還沒打勾」的張數。正常照順序開票時完全不會誤報，測試裡特地驗證了「照順序開 5 張仍無警示」。

警示只在有洞時才出現，多佔一行 20px：

> ⚠ 中間有 **2** 張未打勾　　　　　前往

「前往」跳到順序上最前面一張未打勾的位置，並短暫高亮該列。捲動位置會往下偏移進度列的高度，避免目標列被固定的進度列蓋住。

右邊的「還要開 N 張」與警示的「中間有 M 張」是兩個不同的數字：前者是總共還要開幾張，後者是往下開的過程中跳過了幾張。

---

## 三、客戶臨時改張數 — 這次抓到的實際問題

你提到業務照順序開票是為了「客戶臨時改張數時有調整空間」。我依這個情境測了現行程式，結果是：

> 業務已開好並打勾前 12 張，客戶改口只給 15 張 → 一改張數，**12 個打勾全部清空**。
> 但前 14 張的日期和金額其實一個字都沒變（第 1 張還是 115/07/31、101 元）。

這是我先前做「改動輸入就切斷歷史連結」時的假設錯誤 —— 我當時想的是「改欄位 = 換一批票」，但你描述的是「同一批票，只是張數變了」。

**修正規則**：張數從 N 改成 M 時，真正變動的只有尾款票的位置與金額。所以

- 保留前 **min(N, M) − 1** 張的打勾（這些月票完全沒變）
- 清掉第 **min(N, M)** 張（身分從月票變尾款票或反過來，金額不同）
- 多出來的張數維持未打勾

改繳款金額或開始日期會讓整批的金額或日期改變，打勾一律清空。

---

## 四、存檔流程

### 只打勾 vs 改計算

| 動作 | 行為 |
|---|---|
| 套用紀錄後**只打勾** | 即時寫回原紀錄，不問也不提示 —— 這就是工作紀錄 |
| 改了**張數／金額／日期** | 切斷連結、顯示「已修改，尚未儲存」、保存時才問去向 |

打勾不改變計算內容，每次都跳確認會很煩；改了計算內容才是真的產生分歧。

### 詢問的時機

確認視窗刻意放在**按下「保存計算」時**，而不是改張數的當下。業務改張數時人還在客戶面前、票也還沒開完，那時候問存檔沒有判斷依據，而且模態視窗會打斷正在進行的手寫動作。

按下保存時才問：

> 這筆資料來自「2026-07-25」的紀錄，內容已經變更。
> 原紀錄：20 張 · 尾款 250,000
> 目前：15 張 · 尾款 500,000
> 　　[ 覆蓋原紀錄 ]　[ 另存為新紀錄 ]

覆蓋放前面，因為會走到這個視窗的情境幾乎都是「客戶改張數」，舊紀錄已經不反映事實。

全新的試算不會問，直接存。「以尾款續開」是明確開始新的一批，也不會問。

因為現有的確認框只有兩個按鈕，另外在 `common-keypad.js` 加了共用的 `showChoiceModal()`。

### 自動暫存

「切斷連結」的空窗期（從改張數到按保存之間）打勾只存在記憶體裡，App 被系統回收就沒了。所以加了獨立的暫存槽 `checkCalculatorDraft`，持續寫入表單與打勾，切到背景時也會寫一次，下次打開自動還原。

比照計算頁設 **24 小時效期** —— 暫存的定位是「意外遺失的防護網」而非正式紀錄，真正要留的請按保存計算。隔天打開不會跳出前一天的資料。

### 未儲存進度的攔阻

三個會清掉進度的動作 —— **套用其他紀錄**、**以尾款續開**、**清除全部輸入** —— 在「有打勾且尚未儲存」時會先確認：

> 目前已打勾 **12** 張的開立進度尚未儲存，套用其他紀錄後就會消失。確定要繼續嗎？

沒打勾就直接執行，不會變成每次都要多按一下的噪音。已經寫回紀錄的進度（有連結）也不會攔，因為那些是安全的。

---

## 五、驗證

| 測試 | 項數 | 結果 |
|---|---:|---|
| 大寫金額測試 | 44 | ✓（含 82 萬筆往返比對） |
| 共用鍵盤測試 | 70 | ✓ |
| 計算頁功能測試 | 49 | ✓ |
| 支票頁功能測試 | 56 | ✓ |
| 支票開票流程測試 | 81 | ✓ |
| **支票進度與存檔測試（新增）** | **80** | ✓ |
| 鍵盤行為對照測試 | 約 30 萬次按鍵 | ✓ 零不一致 |
| **合計** | **380 項** | **全過** |

新增的 `支票進度與存檔測試.js` 涵蓋這一批的全部行為，其中幾項值得一提：

**改張數的雙向驗證** —— 20→15 保留 12 個勾、10→14 保留前 9 個且原尾款票的勾被清掉。兩個方向都測，因為規則對稱但很容易只做對一邊。

**漏開偵測不誤報** —— 照順序開 5 張時必須完全沒有警示。這是這個功能最容易做壞的地方：如果判斷寫成「有未打勾就警示」，業務從第一張開始就會被洗版。

**暫存的效期邊界** —— 25 小時不還原且清除、23 小時正常還原。

**攔阻的取消路徑** —— 跳出確認後按取消，打勾必須原封不動。只測「確定」不夠，取消才是使用者按錯時的保護。

另外也做了靜態檢查：所有 JS 通過 `node --check`；頂層名稱重複偵測（同頁載入的多個檔案間若有同名 `let` 會整個腳本停止執行）計算頁 74 個、支票頁 99 個名稱皆無衝突。

---

## 六、目前狀態

| 檔案 | 行數 |
|---|---:|
| `pages/check.html` | 227 |
| `css/check.css` | 623 |
| `js/check-engine.js` | 1,545 |
| `js/common-keypad.js` | 521 |

原健檢報告的 A 級 7 項、B 級 5 項已全部完成，C 級大多也在這幾批一併處理掉了。

**還沒做的：**

- **C3** 底部尾款卡片與上方欄位資訊重複 —— 等你決定去留
- **C5** 月曆在 2026/1、2026/10 只有 5 列，翻月時彈窗高度會跳動
- **C6** 日期選擇器打開時「今天」不會被選中；以及是否允許選過去日期
- **C12** GA 事件完全沒追蹤

---

## 七、建議下一步

先實機試用這一批。這次改的東西有幾項只有真的操作才知道好不好：

- 固定進度列在捲動時會不會擋到清單第一列
- 金額群組外框讓版面變高了一點，整體是否還夠緊湊
- 漏開警示跳出來時會不會覺得突兀
- 改張數後保留打勾的行為，是否符合當下的直覺

試用後如果沒問題，剩下的 C5、C6 都是日期選擇器的問題，可以一起處理。

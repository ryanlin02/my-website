# GA4 後台設定清單

這份文件是給「已經打開 GA4 後台、但不想碰程式」的人照著做的。
全部都是在網頁上點選，不需要改任何程式碼。

**最重要的一句話：自訂維度不會回溯。** 登記之前送進來的資料，
在報表上永遠看不到細節。所以請盡早做完下面第二節。

---

## 一、先確認資料保留期限（你已經完成）

位置：**管理 → 資料收集和修改 → 資料保留**

設定成 **14 個月**（或更長）。預設值只有 2 個月，過期永久刪除、救不回來。

> 你已經確認過是 14 個月，這一節可以跳過。

---

## 二、登記自訂維度（重要，請優先做）

位置：**管理 → 資料顯示 → 自訂定義 → 自訂維度 → 建立自訂維度**

每一列都要按一次「建立自訂維度」，填三個欄位：

- **維度名稱**：報表上會看到的名字，填中文比較好認
- **範圍**：一律選 **事件**
- **事件參數**：一定要跟下表**完全一樣**，一個字都不能差（大小寫也是）

### 一定要登記的（共 9 個）

| 維度名稱 | 事件參數 | 這個能回答什麼問題 |
|---|---|---|
| 功能頁 | `tool` | 四個功能誰最常被用、誰沒人碰 |
| 來源功能頁 | `from_tool` | 業務的工作順序，例如是不是算完貸款就去開支票 |
| 開啟方式 | `display_mode` | 裝了應用程式的人 vs 用瀏覽器的人，誰用得比較兇 |
| 瀏覽器種類 | `browser_kind` | 有多少人是從 LINE 裡面點進來的 |
| 裝置類型 | `platform` | iPhone / Android / 電腦的比例 |
| 進入頁 | `entry_tool` | 業務打開工具箱時最常停在哪一頁 |
| 試算方式 | `calc_mode` | 算月付金比較多，還是反推利率比較多 |
| 期數 | `period` | 業務常談的方案長度落在哪 |
| 支票張數 | `check_count` | 常見的開票張數 |

### 有空再登記的（共 6 個）

| 維度名稱 | 事件參數 | 這個能回答什麼問題 |
|---|---|---|
| 利率 | `rate` | 常談的利率區間 |
| 發票聯式 | `invoice_type` | 三聯式與二聯式的比例 |
| 分享方式 | `share_method` | 業務怎麼把發票傳給客戶 |
| 統編查到了嗎 | `found` | 離線統編索引夠不夠用，查不到的比例高就該補資料 |
| 油價來源 | `source` | 加油頁的快速鍵和油價表誰比較常用 |
| 錯誤訊息 | `error_message` | 業務手機上出了什麼狀況 |

登記完之後，資料要等 **約 24 小時** 才會出現在一般報表裡。
想馬上確認有沒有送出去，看下一節。

---

## 三、怎麼確認資料真的有進來

### 立刻可以看的

位置：**報表 → 即時**

用手機或電腦打開工具箱操作幾下，畫面上的「事件計數」會在幾秒內跳動。
看得到 `app_open`、`tool_view` 這些名字就代表通了。

### 隔天之後可以看的

位置：**報表 → 參與 → 事件**

這裡會列出所有事件與次數。想看細節（例如「哪個功能最常用」），
點進 `tool_view`，把上面登記好的「功能頁」拉進來當維度。

---

## 四、這個網站現在會送出哪些事件

### 整體

| 事件名稱 | 什麼時候送 |
|---|---|
| `app_open` | 每次打開工具箱 |
| `tool_view` | 每次切到某個功能頁（含開啟時停留的第一頁） |
| `page_view` | 同上，讓「網頁和畫面」報表能分開四個功能 |
| `javascript_error` | 網頁發生錯誤 |
| `device_info` | 開啟時記錄一次螢幕與裝置資訊 |
| `pwa_install_prompt` / `pwa_install_choice` | 跳出安裝視窗、以及使用者的選擇 |
| `update_available` / `update_applied` / `update_dismissed` | 新版本提示與使用者的反應 |

### 計算頁

| 事件名稱 | 什麼時候送 |
|---|---|
| `loan_calculate` | 完成一次試算（算月付金或反推利率） |
| `loan_saved` | 把結果存進歷史 |
| `loan_history_loaded` | 從歷史叫回舊案件 |

### 支票頁

| 事件名稱 | 什麼時候送 |
|---|---|
| `check_calculated` | 算出尾款金額（只在從無到有那一刻送一次） |
| `check_saved` | 保存計算進歷史 |
| `check_history_loaded` | 套用歷史紀錄 |
| `check_all_written` | 整批票全部開完打勾 |

### 發票頁

| 事件名稱 | 什麼時候送 |
|---|---|
| `invoice_saved` | 存檔 |
| `invoice_image_saved` | 存成圖片或分享圖片 |
| `invoice_shared` | 分享連結給同事 |
| `taxid_lookup` | 查統一編號（只記查到或查不到） |

### 加油頁

| 事件名稱 | 什麼時候送 |
|---|---|
| `page_specific_load` | 頁面載入 |
| `fuel_price_selected` | 選定油品單價 |
| `discount_calculated` | 完成一次折扣試算 |
| `calculation_cleared` | 按下清除 |

---

## 五、隱私原則（已經寫進程式，也有測試在守）

以下資料**一律不會**送進 GA4：

- 貸款本金、月付金、佣金、總繳款
- 支票的總金額、繳款金額、尾款金額
- 發票的金額、抬頭、統一編號
- 客戶姓名、車號

送出去的只有「業務做了什麼動作」和「期數 / 張數 / 幾聯式」這種
不指向特定客戶的資訊。

`tests/GA4事件測試.js` 裡有一項專門檢查這件事，
未來若有人不小心把金額欄位加進追蹤，測試會直接失敗。

---

## 六、2026/07 這次修正了什麼

在此之前，這個網站的統計數字幾乎不能用：

1. **瀏覽次數被灌水五倍** — 四個功能頁在開啟時同時載入，
   每一頁都自己回報一次瀏覽，而且四頁數字永遠一樣，
   完全看不出誰常用。**已修正**：瀏覽紀錄改由外殼統一送。

2. **切頁從來沒被記錄過** — 負責回報切頁的程式被同名函式蓋掉。
   **已修正**：改由切頁函式直接回報。

3. **錯誤記錄從來沒運作過** — 前面一行去找不存在的元素，
   程式一出錯整段停住。**已修正**：錯誤記錄獨立出來。

4. **計算頁把每顆按鈕都回報** — 數字鍵盤也算按鈕，
   算一筆貸款送十幾筆「按了 7」。**已移除**。

5. **支票頁與發票頁完全沒有追蹤** — 一筆資料都沒有。
   **已補上**，各四個有意義的事件。

6. **停留時間與切換 App 自己寫了一份** — GA4 內建就有，
   自己寫的比較不準還會讓報表出現兩個對不起來的數字。**已移除**。

# 修改計劃：存檔與歷史紀錄的體驗統一

更新日期：2026-07-30
範圍：**只做存檔與歷史的使用者體驗統一**。跨頁連動、方案比較屬於未來規劃，這次不做，但架構上預留不擋路。

---

## 已定案的設計決定

- **統一的方向是「把支票頁推廣到其他三頁」**，不是重新發明。支票頁的模式（草稿與歷史分開、覆蓋或另存、寫入失敗會提示、id 不會撞）是四頁裡演化最完整的。
- **四頁全部納入**，包含加油頁（現在完全沒有存檔功能，會是改動最多的一頁）。
- **清單默認收合、點一下展開**。收合時一筆兩行，展開後才出現「套用／備註／刪除」。順便降低誤觸刪除的機率，也讓套用鈕自然落在拇指剛點過的位置。
- **容器維持全螢幕**（計算頁與發票頁本來就都是全螢幕，只是 class 名字不同），但捲動結構統一採發票頁的做法：標題列用 flex 固定，不用 `sticky`。
- **關閉鈕維持右上角**。主要動線是「開歷史 → 找到 → 套用 → 自動關閉」，`×` 是比較少走的路，不值得為它改版面。
- **「清空歷史」移到清單最底部**，不再跟 `×` 並排。破壞性動作不該放在關閉鈕旁邊。
- **用詞定案**：存檔 ／ 歷史 ／ 套用 ／ 清空歷史。
- **localStorage key 名稱不改**。統一「程式怎麼存取」就夠了，改 key 只有維護上的好處，卻要冒資料搬遷的風險。

---

## 步驟 1｜修三個會弄丟資料的問題　✅ 已完成（2026-07-30）

無視覺變動，跟後面選哪條路無關，做完可立刻上線。

- [x] 發票頁 `deleteRecord()` 補確認彈窗（帶出抬頭與金額，刪錯看得出來）
- [x] 發票頁「全部刪除」補確認彈窗（帶出筆數）
- [x] 計算頁 `saveLoanData()` 的 `setItem` 加 try/catch 與失敗提示
- [x] 計算頁 id 由 `new Date().getTime()` 改為 `Date.now()*1000 + 亂數`
- [x] **（計劃外）** `.modal-overlay` z-index 2000 → 5500

**動到**：`js/invoice-engine.js`、`js/calc-storage.js`、`css/keypad.css`、新增 `tests/存檔安全性測試.js`

> **為什麼多做了 z-index**：發票頁的刪除確認是從歷史面板裡叫出來的，而共用彈窗
> 原本是 2000、發票頁的 `.sheet` 是 4500 —— 彈窗確實開了，但整個被面板蓋住。
> 不一起改的話，這步交出去的是一個「點了完全沒反應」的確認鈕，比原本直接刪還糟。
> 這也證實了原本排在步驟 2 的 z-index 問題會比預期更早咬人。
> 步驟 2 仍要做四頁完整的疊層收斂，這裡只先解掉會被蓋住的部分。

**怎麼驗**：`node tests/存檔安全性測試.js`（22 項）＋既有 15 支測試全過。
新測試做過突變驗證：把三個修正各自改回舊版，對應的項目確實會亮紅燈——
其中 id 改回舊版時，刪一筆會把同毫秒存的三筆**全部刪光**，實際危害比預估更大。

---

## 步驟 2｜建立 `js/common-history.js`（UI 完全不動）　✅ 已完成（2026-07-30）

使用者看不到任何變化，但這是後面每一步的地基。

- [x] Store API：`list / get / save / patchData / setNote / remove / clear / count`
- [x] 共同外殼：`{ v, id, tool, savedAt, note, customer, data:{...各頁原本欄位} }`
- [x] 四頁改成呼叫 Store（key 沿用舊的，一個都沒改名）
- [x] 舊資料相容：**不列舉欄位名稱**，扣掉外殼欄位後其餘全部進 `data`，所以連沒想到的欄位也不會掉；遷移只在讀取時發生，不改寫磁碟上的舊資料
- [x] z-index 收斂成 `keypad.css` `:root` 的五層變數
- [x] 清死碼：`loanCalculatorEmergencyBackup`（全站只有 get／remove，沒有任何 set）
- [x] **（計劃外）** 備註編輯器樣式從 `calculator.css` 搬到 `keypad.css`
- [x] **（計劃外）** 補 `--accent-color-light` 到 `keypad.css`
- [x] **（計劃外）** 修正兩支測試漏列 `keypad.css` 的頁面-CSS 對照表
- [x] `sw.js` 的預快取清單加入 `common-history.js`（版本號不手動改，由 GitHub Actions 自動遞增）

**動到**：新增 `js/common-history.js`；四頁 HTML、`js/calc-storage.js`、`js/check-engine.js`、`js/invoice-engine.js`、`css/keypad.css`、`css/calculator.css`、`css/invoice.css`、`sw.js`

**怎麼驗**：新增 `tests/歷史紀錄遷移測試.js`（54 項，Store 層）與 `tests/舊資料相容測試.js`（38 項，真實頁面）。後者是關鍵——它把改版前的真實格式塞進 localStorage，把頁面整個跑起來，檢查畫面上的數字。突變驗證確認：把頁面改成讀錯路徑（`rec.xxx` 而非 `rec.data.xxx`）時，畫面會出現 `undefined` 與 `NaN` 但**不會拋任何錯誤**——這正是這支測試要擋的無聲失敗。

### 這一步實際踩到的三件事

**一、`.note-editor-modal` 不是死碼，而且放錯地方了。**
計劃裡我猜 `calculator.css` 那個 z-index 2500 的遮罩是舊版備註編輯器殘留。查了才發現它是**現役**的共用備註編輯器，由 `common-keypad.js` 建立、四頁共用——但樣式只寫在 `calculator.css`，而那份只有計算頁與支票頁載入。**發票頁與加油頁一旦加備註（步驟 4），會拿到一個完全沒有樣式的對話框**，連 `display:none` 都沒有。已搬到 `keypad.css`。

**二、搬完之後 `--accent-color-light` 變成懸空引用。**
`.note-editor-input:focus` 用到它，但它只宣告在 `calculator.css`。CSS 遇到未宣告的變數會整條靜默失效。這是既有測試抓到的。

**三、兩支測試的「各頁載入哪些 CSS」對照表都漏了 `keypad.css`。**
四頁其實都載入它。漏掉的後果是雙向的：`keypad.css` 宣告的變數被誤判成沒人宣告，它自己引用的變數也完全沒被檢查到。修好之後才抓得到第二點。

### 順帶修掉的一個設計缺陷

支票頁當初為了解決 id 碰撞而加亂數，確實不撞了，但**同一毫秒內產生的 id 大小順序是隨機的**。時間戳相同時排序只能靠 id 決勝負，於是「哪一筆比較新」變成隨機——超過筆數上限要裁掉最舊的時候，裁掉的是隨機一筆。遷移測試就是這樣抓到的（存 0~4 五筆、上限 3，留下 4,1,0）。改成流水號後 id 嚴格遞增。

---

## 步驟 3｜用詞統一　✅ 已完成（2026-07-30）

- [x] 按鈕：保存計算／存檔 → **存檔**
- [x] 入口：歷史記錄／歷史 → **歷史**
- [x] 面板標題：歷史記錄／支票計算歷史記錄／歷史記錄 → **歷史紀錄**
- [x] 載入：載入計算／套用資料 → **套用**
- [x] 全刪：全刪／全部刪除 → **清空歷史**
- [x] 確認彈窗標題：確認刪除／刪除確認 → **刪除確認**；確認刪除／全刪確認／清空確認 → **清空確認**
- [x] 提示訊息：**已存檔／已套用／已刪除／已清空歷史／備註已儲存**
- [x] 名詞一律用「**紀錄**」（記錄是動詞、紀錄是名詞，原本四頁混用）
- [x] **（計劃外）** 三頁的確認彈窗內容也統一：單筆刪除都帶出該筆摘要、清空都帶出筆數
- [x] **（計劃外）** 三份說明頁同步更新

**動到**：四頁 HTML、三支 engine、三份 `instructions/*.html`、新增 `tests/用詞一致性測試.js`

**怎麼驗**：`node tests/用詞一致性測試.js`（54 項）＋既有 26 支全過。突變驗證：把任一頁的按鈕改回舊說法，測試會指名是哪個檔案的哪個字。

### 為什麼順便改了說明頁與確認彈窗

**說明頁不改就會變成新的不一致。** 三份說明頁逐字寫著「點擊『保存計算』按鈕」，按鈕改名之後使用者照著說明會找不到。而且查下去發現**說明頁本來就有兩處是錯的**：`check_instruction.html` 寫「載入計算」，但支票頁畫面上一直是「套用資料」；`invoice_instruction.html` 描述了一顆「查看」按鈕，發票頁上根本沒有這顆按鈕（目前是點整列載回）。

**確認彈窗只統一標題會做半套。** 原本三頁的訊息內容差很多：發票頁會告訴你要刪掉的是哪一筆、共幾筆，計算頁與支票頁只說「確定要刪除嗎」。使用者按下去之前無從確認自己點對了列——而這是不可復原的動作。三頁現在都會帶出摘要與筆數。

---

## 步驟 4｜補齊各頁缺的功能

全是加法，不動既有行為。步驟 2 已經把能力做在 Store 層，各頁只是接上去。

### 4-1　發票頁與計算頁　✅ 已完成（2026-07-30）

- [x] 發票頁：歷史清單一列改成兩行，補**備註**與明確的**「套用」按鈕**
- [x] 發票頁：刪除鈕由小小的 `×` 改成文字「刪除」，與另外兩頁一致
- [x] 發票頁：補 24 小時草稿自動暫存（四頁裡原本唯一沒有的）
- [x] 計算頁：補覆蓋／另存，含「已修改，尚未儲存」提示
- [x] **（計劃外）** `.unsaved-hint` 與 `.choice-modal-footer` 從 `check.css` 搬到 `keypad.css`

**動到**：`js/invoice-engine.js`、`js/calc-storage.js`、`js/calc-ui.js`、`pages/calculator.html`、`css/invoice.css`、`css/check.css`、`css/keypad.css`

**怎麼驗**：新增 `tests/存檔功能對齊測試.js`（46 項）。突變驗證三種改壞方式都會亮紅燈：拿掉「使用者改了內容」的標記、發票頁點備註沒攔下來（會順便把該筆套用回表單）、共用樣式搬離 `keypad.css`。

> **又踩到同一個坑**：`.unsaved-hint`（已修改提示）和 `.choice-modal-footer`
> （覆蓋／另存對話框在窄螢幕的堆疊規則）都只寫在 `check.css`，而計算頁
> 不載入那份。這是步驟 2 備註編輯器那件事的第三次重演 ——
> **共用元件的樣式散落在單一頁面的 CSS 裡**。已一併搬到 `keypad.css`。
> 步驟 5 應該把剩下的共用樣式一次盤完，不要再一次踩一個。

### 4-2　加油頁　✅ 已完成（2026-07-30）

- [x] 補存檔、歷史面板、備註、覆蓋／另存、「已修改」提示、24 小時草稿
- [x] 卡片顯示**每月油錢**與**折扣金額**（業務自己在意的數值），徽章放單價
- [x] **（計劃外）** 新增 `css/history.css`：把歷史面板與存檔按鈕列的樣式從 `calculator.css` 抽出，四頁共用
- [x] **（計劃外）** 補 `--note-text-color` / `--note-text-empty` 到 `keypad.css`
- [x] **（計劃外）** 兩支測試的頁面-CSS 對照表補上 `history.css`

**動到**：`js/gas-engine.js`、`pages/gas.html`、新增 `css/history.css`、`css/calculator.css`、`css/keypad.css`、四頁 HTML、`sw.js`

**怎麼驗**：新增 `tests/加油頁存檔測試.js`（76 項）。突變驗證：把套用時的換算方向設反，測試會抓到每月油錢從 290,000 變成 289,987。

> **卡片為什麼不放「每月節省」**：那是講給客戶聽的數字。業務回頭找舊紀錄時
> 要看的是自己輸入過什麼（油錢、折扣），節省金額在套用回表單時會即時重算。

> **換算方向是這頁最容易出事的地方**：油錢與油量是雙向換算，方向由
> `lastModifiedField` 決定。套用歷史時沒指定成「油錢」的話，引擎會拿油量
> 反算油錢，把剛還原的值蓋掉。測試刻意用除不盡的金額（290,000 ÷ 28.8）
> 才驗得出來——用整除的數字兩個方向結果一樣，測不出差別。

### 共用樣式的第四次（也是最後一次）搬遷

這次做完了原本說要做的完整盤點。結果是整組歷史面板樣式（21 個 class）
＋存檔按鈕列都只在 `calculator.css`，而那份只有計算頁與支票頁載入。

所以這次不是再搬一個 class，而是**抽成獨立的 `css/history.css`，四頁一起載入**。
步驟 5 要重做清單版面時，也只要改那一份。

同一個病共出現四次：備註編輯器 → 已修改提示 → 多選項對話框 → 整組歷史面板。
根因是「共用元件的樣式散在單頁 CSS 裡」，現在四頁載入的檔案是：

```
keypad.css    共用元件：鍵盤、彈窗、備註編輯器、提示、疊層變數、色彩 token
history.css   共用元件：歷史面板、紀錄卡片、存檔／歷史按鈕列
<頁面>.css    只放這一頁真正專屬的東西
```

**相依**：必須在步驟 2 的 z-index 收斂之後，否則備註彈窗會被歷史面板蓋住

**排在步驟 5 之前是有意的**——先把功能補齊，版面步驟才知道卡片裡到底要塞什麼。反過來做會變成先設計好卡片、再發現發票頁多一行備註要塞。

---

### 4-3　收尾三項　✅ 已完成（2026-07-30）

- [x] **加油頁紀錄瘦身**：只存單價、每月油錢、折扣金額三個輸入值；油量、折後油錢、每月／每年節省一律套用時重算；卡片也拿掉那兩個推導值
- [x] **存檔時間統一顯示**：`common-history.js` 新增 `formatSavedAt()`，四頁共用
- [x] **發票頁補覆蓋／另存**，判定為「改任何一項都算」
- [x] 計算頁、支票頁、加油頁不再自己存格式化過的日期字串

**動到**：`js/common-history.js`、四支 engine、`pages/invoice.html`、`css/invoice.css`

**怎麼驗**：`存檔功能對齊測試`（63 項）、`加油頁存檔測試`（81 項）、`歷史紀錄遷移測試`（60 項）。突變驗證三項都會亮紅燈：發票頁 `touch()` 不標記變更、把標記誤掛到 `render()`（自動帶入抬頭會被當成使用者修改）、時間格式加上秒數。

> **為什麼不存推導值**：一筆紀錄同時存輸入值與從它算出來的值，等於讓同一件事有兩個真相來源；兩者對不起來時程式就得決定要相信哪一個。加油頁的油錢與油量是雙向換算，方向設錯就會拿油量覆蓋剛還原的油錢，**數字被改掉了畫面上還看不出來**。只存輸入值之後，真要出錯也會是「下面幾格整片空白」這種一眼看得見的壞法。

> **時間為什麼不到秒**：沒有人記得自己是 14:30:45 跟客戶談的。秒數對回想沒有幫助，卻要多佔 3 個字元，而那一行右邊緊接著徽章。改用「今天／昨天」反而更短也更好認。

> **發票頁的判定與排除**：改任何一項都算變更，但統編查詢自動帶出的抬頭不算——那是程式改的。實作上標記掛在 `touch()` 而不是 `render()`，自動帶入走的正好是 `render()`，天然被排除。

### 四頁對照（步驟 4 完成）

| 能力 | 計算 | 支票 | 發票 | 加油 |
|---|:--:|:--:|:--:|:--:|
| 存檔（走共用 Store） | ✓ | ✓ | ✓ | ✓ |
| 歷史面板 | ✓ | ✓ | ✓ | ✓ |
| 套用按鈕 | ✓ | ✓ | ✓ | ✓ |
| 備註 | ✓ | ✓ | ✓ | ✓ |
| 刪除／清空確認 | ✓ | ✓ | ✓ | ✓ |
| 24 小時草稿 | ✓ | ✓ | ✓ | ✓ |
| 覆蓋／另存 | ✓ | ✓ | ✓ | ✓ |
| 「已修改」提示 | ✓ | ✓ | ✓ | ✓ |
| 存檔時間顯示 | ✓ | ✓ | ✓ | ✓ |

**四頁的能力與用詞至此完全對齊。** 剩下的只有版面長相（步驟 5）。

---

## 步驟 5｜歷史面板統一（視覺變動最大）

### 5-1　收合／展開機制 ＋ 支票頁　✅ 已完成（2026-07-30）

- [x] `common-history.js` 加入展開狀態管理（`isHistoryExpanded` / `historyExpandedClass` / `setupHistoryExpand` / `forgetHistoryExpanded`）
- [x] `history.css` 加入收合／展開樣式，含會轉向的展開箭頭
- [x] 支票頁卡片改為收合兩行：**存檔時間＋開立進度｜總金額＋張數＋備註**
- [x] 六個細項與套用／備註／刪除全部收進展開層

**動到**：`js/common-history.js`、`js/check-engine.js`、`css/history.css`

**怎麼驗**：新增 `tests/歷史卡片收合測試.js`（31 項）。突變驗證三種改壞方式都會亮紅燈：細項留在收合層（等於沒收合）、CSS 沒把 detail 藏起來、展開狀態沒記在模組層（重繪後自己合起來）。

> **為什麼既有測試抓不到這件事**：支票頁原本的測試只檢查「六個欄位在不在 DOM 裡」，而收合之後那六個欄位**仍然在 DOM 裡**，只是被 CSS 藏起來。就算收合完全沒生效，既有測試依然全綠。新測試守的是「哪些資訊在哪一層」。

> **展開狀態為什麼要記在模組層**：存備註、打勾、刪除都會讓面板整個重畫。狀態若只寫在 DOM 上，使用者展開的那一筆會在存完備註後自己合起來。

> **不做手風琴**：展開一筆不會把其他筆關掉。手風琴雖然更省空間，但「我剛剛展開的那筆自己關了」是會讓人困惑的行為。

### 5-1b　編輯模式與多選刪除（支票頁先行）　✅ 已完成（2026-07-30）

一次解決兩個問題：**刪多筆的成本**與**「清空歷史」緊鄰關閉鈕**。

- [x] `common-history.js` 加入編輯模式狀態、多選、全選／取消全選、多筆刪除
- [x] `history.css` 加入雙態標題列與勾選框樣式
- [x] 支票頁標題列改為雙態；移除 `confirmDeleteAll()`
- [x] **（順手修）** 全站移除 `font-style: italic`

```
一般模式   歷史紀錄            [編輯] [×]      ← 沒有任何破壞性按鈕
編輯模式   已選 N 筆   [全選] [刪除] [完成]
```

**怎麼驗**：`tests/歷史卡片收合測試.js`（66 項）。突變驗證：編輯模式下點列沒有切換成選取、刪除鈕少了停用保護、標題列被加回破壞性按鈕——都會亮紅燈。

> **刪多筆的成本**：一筆要點三次（展開 → 刪除 → 確定），清五筆就是十五次。編輯模式下是八次，筆數越多省越多。

> **為什麼不用常駐勾選框**：收合行本來就緊（時間、進度、金額、張數、備註），常駐一個平常用不到的勾選框會把備註擠掉——而備註是找紀錄時的辨識線索。

> **「清空歷史」不再單獨存在**：「全選 → 刪除」完成同一件事，而且更安全（看得到選了幾筆）。為同一件事保留兩個入口只是多一個誤觸點。

> **中文不用斜體**：中文字體沒有真正的斜體字形，瀏覽器只能合成傾斜，傾斜後最後一個字的右上角會被 `overflow: hidden` 切掉（實機上「台積電」的「電」缺了一角）。全站三處斜體都同時有 `overflow: hidden`，已全部改用顏色弱化並加守門測試。

### 5-1c　展開層改為同一橫列　✅ 已完成（2026-07-30）

- [x] 標籤與數值同列（標籤 12px 灰／數值 14px 白粗、等寬數字）
- [x] 修正反過來的視覺層級（舊版標籤 16px 大於數值 14px）
- [x] 拿掉三層「框中框」：標題列、欄位區、備註區各自的底色與外框
- [x] 時間與徽章的左右邊距歸零，邊緣與下方摘要行對齊
- [x] 收合行字級：金額 15→17px、張數與備註 11→12px；徽章 16→14px

> 展開的高度從約 122px 降到約 71px。這組樣式原本寫在 `calculator.css`，
> 是計算頁舊卡片沿用下來的，抽成共用檔時照搬未改。

### 5-2　推到另外三頁　✅ 已完成（2026-07-30）

- [x] 計算頁：收合行 **存檔時間＋稅佣後利率｜本金＋期數**
- [x] 加油頁：收合行 **存檔時間＋單價｜每月油錢＋折扣金額**
- [x] 發票頁：**存檔時間＋聯式｜總計＋抬頭**；原本橫向一列的 `.hrec` 結構
      整套換成共用的 `.history-item`，`invoice.css` 的舊樣式一併移除
- [x] 四頁標題列全部改為雙態，`confirmDeleteAll()` 四頁皆已移除

**動到**：四支 engine、四頁 HTML、`css/invoice.css`

**怎麼驗**：`tests/歷史卡片收合測試.js`（98 項），新增「四頁一致性」段落——四支渲染函式都必須輸出兩層結構、掛 `data-history-id`、呼叫 `setupHistoryPanel()`，且不再自己處理清空。

> **發票頁的舊樣式一定要刪**：`.hrec` 那一整套換掉之後就沒人用了，
> 留著只會變成「沒人用卻可能被層疊順序喚醒」的死碼——
> 步驟 4-2 的 `.bottom-buttons button` 就是這樣把按鈕外觀改掉的。

### 5-3　其餘版面項目（待做）

- [ ] 標題列改用 flex 固定，不用 `sticky`（捲動時完全不晃）
- [ ] 「清空歷史」移到清單最底部，不再跟關閉鈕並排
- [ ] 存檔／歷史按鈕位置四頁一致

---

## 步驟 6｜搜尋（之後再看）

等歷史真的長到會捲不完再做，現在做可能是想像出來的需求。做的時候用金額或期數當關鍵字，走現有的數字鍵盤，不會叫出系統鍵盤。

---

## 相依關係

```
1 ──（獨立，隨時可做，可先上線）
2 ── 必須先於 3、4、5
     └ 其中 z-index 收斂必須先於 4 的發票頁備註
4 ── 必須先於 5
```

## 驗收標準

四頁的存檔與歷史，以下六項完全一致：按鈕文字、按鈕位置、面板長相與捲動行為、刪除確認、備註、草稿還原行為。

## 未來連動的預留（已包在步驟 2 裡）

1. 每筆紀錄都有共同外殼，未來連動只是在外殼加一個欄位，不用動任何一頁的計算邏輯
2. 只有一支檔案碰得到儲存，未來要加跨頁查詢或換儲存方式只改那一支
3. 外殼有 `customer` 欄位先留空，現在只有發票頁會填（那頁本來就要打統編），未來靠它把四頁紀錄認成同一個客戶

---

## 附錄：現況盤點（施工時對照用）

| 項目 | 計算頁 | 支票頁 | 發票頁 | 加油頁 |
|---|---|---|---|---|
| 存檔按鈕位置 | 頁面中段 | 頁面底部 | 固定頂列 | 無 |
| 按鈕文字 | 保存計算 | 保存計算 | 存檔 | — |
| 歷史容器 | `.history-panel` 全螢幕 | 同左（借用 calculator.css） | `.sheet` 全螢幕 | — |
| 容器 z-index | 1500 | 1500 | **4500** | — |
| localStorage key | `loanHistory` | `checkHistory` | `invNewHistory` | — |
| 筆數上限 | 無 | 無 | 100（靜默丟棄最舊） | — |
| 套用按鈕 | 「載入計算」 | 「套用資料」 | **無，點整列** | — |
| 刪除確認 | 有 | 有 | **無** | — |
| 備註 | 有 | 有 | **無** | — |
| 覆蓋原紀錄 | **無** | 有 | **無** | — |
| 24h 草稿 | 有 | 有 | **無** | **無** |
| 寫入失敗處理 | **無** | 有 | **無** | — |
| id 生成 | `getTime()`（會撞） | `Date.now()*1000+亂數` | `Date.now()` | — |

共用彈窗 z-index = 2000、toast = 9999（`css/keypad.css`）。

# 修改計劃 — 加油頁（gas.html）

> 建立日期：民國 115 年 07 月 26 日
> 對象檔案：`pages/gas.html`（1736 行 / 73KB，CSS 與 JS 全內嵌）
> 目前版本：`4.35`（sw.js CACHE_VERSION）
> 狀態：**尚未執行任何修改**

---

## 一、為什麼要改

加油頁是四個功能頁裡唯一沒有經過重構的頁面。其他三頁的 CSS 與 JS 都已拆到 `css/` 與 `js/`，加油頁還停留在單檔內嵌（CSS 753 行、JS 583 行），且累積了兩個會**在客戶面前顯示錯誤數字**的實質 bug。

| 頁面 | 外部 CSS | 共用鍵盤 | 共用彈窗 | 有測試 |
|---|---|---|---|---|
| 計算 | ✅ | ✅ | ✅ | ✅ |
| 支票 | ✅ | ✅ | ✅ | ✅ |
| 發票 | ✅ | 自有 | 自有 | ✅ |
| **加油** | ❌ | ❌ | ❌ | ❌ |

---

## 二、執行原則

這份計劃刻意切成**六個獨立階段**，每階段都能單獨完成、單獨驗證、單獨回退。

1. **一階段 = 一次 commit。** 不要把兩個階段合併提交，出錯時才能精準回退。
2. **每階段結束先跑驗收清單**，全部通過才進下一階段。
3. **階段 1、3、5、6 不動架構；階段 2 只搬移不改邏輯；階段 4 才動共用模組。** 風險是遞增的。
4. **每階段完成後執行版本遞增**，否則手機拿不到新版：
   ```bash
   python3 scripts/bump-version.py
   ```
   （會同步更新 `sw.js` 的 CACHE_VERSION 與 `manifest.json` 的 version）
5. **回退方式**：`git revert <該階段的 commit>`。因為每階段獨立，revert 不會牽連其他改動。

### 已確認的兩個決策

- **「每月油量」欄位** → 恢復**雙向推算**（改油量反算油錢、改油錢或單價則算油量）。
- **併入共用鍵盤** → 納入計劃，但**排在最後（階段 4）**，前面階段穩定後再評估是否執行。

---

## 三、階段規劃總覽

| 階段 | 內容 | 風險 | 預估改動量 | 相依 |
|---|---|---|---|---|
| ~~0~~ | ~~準備：建立測試檔~~ **✅ 完成** | 無 | 新增 1 檔（72 項測試） | — |
| ~~1~~ | ~~Bug 修復~~ **✅ 完成（v4.36）** | 低 | +55 / −71 行 | 階段 0 |
| ~~2~~ | ~~拆檔（CSS / JS 外部化）~~ **✅ 完成（v4.37）** | 低 | 搬移 1328 行 | 階段 1 |
| 3 | GA4 瘦身 | 低 | 刪約 50 行 | 階段 2 |
| 5 | UX 調整 | 中 | 約 40 行 | 階段 3 |
| 6 | 死碼清理 | 極低 | 刪約 30 行 | 階段 3 |
| 4 | 併入共用鍵盤 | **高** | 重寫鍵盤層 | 階段 2、3 |

> 注意：階段編號依「建議執行順序」排列，階段 4 刻意放到最後執行。

---

## 階段 0 — 準備：先建立測試網

**目的**：加油頁目前**沒有任何測試**。在動手改之前先寫測試，才有辦法確認「改完沒弄壞別的」。這是整份計劃唯一的前置作業。

### 要做的事

新增 `tests/加油頁功能測試.js`，比照 `tests/計算頁功能測試.js` 的寫法（jsdom + 走真實路徑）。

**執行方式**（沿用 `tests/README.md` 既有慣例）：
```bash
npm install jsdom          # 若尚未安裝
node tests/加油頁功能測試.js
```

**測試必須涵蓋的項目**（這些就是後續每階段的驗收基準）：

| # | 測試項目 | 目前預期結果 |
|---|---|---|
| 1 | 載入後自動帶入中油柴油單價 | 通過 |
| 2 | 點「中」/「塑」按鈕設定單價 | 通過 |
| 3 | 輸入油錢 → 自動算出油量 | 通過 |
| 4 | **輸入油量 → 自動算出油錢** | **失敗（bug）** |
| 5 | 設定折扣 → 算出折後油錢／每月節省／每年節省 | 通過 |
| 6 | **折扣歸 0 → 結果區必須清空** | **失敗（bug）** |
| 7 | 千分位格式正確（1,000,000） | 通過 |
| 8 | 折扣金額上限 5.0、下限 0.0 | 通過 |
| 9 | 單價保留 1 位小數 | 通過 |
| 10 | 清除鍵清空六個欄位、保留單價 | 通過 |
| 11 | 油價表點擊帶入單價 | 通過 |
| 12 | fuel-prices.json 讀取失敗時走 fallback | 通過 |

> 第 4、6 項刻意**先寫成失敗**。階段 1 修完後這兩項要轉綠 —— 這就是階段 1 的驗收依據。

### ✅ 階段 0 已完成（實際結果與計劃的三處差異）

**1. 改用 XFAIL 而非單純 FAIL。** 原計劃寫「2 項 FAIL」，但那會讓測試的結束碼永遠是 1，失去當關卡的意義。改成標記 `XFAIL`（預期失敗）：不影響結束碼，修好後會自動變成 `XPASS` 並提示把 `xt` 改成 `t`。每個 XFAIL 都標了所屬階段。

**2. 實際涵蓋範圍比計劃大。** 最終是 **50 項 PASS / 12 項 XFAIL / 結束碼 0**，共 16 個測試群組。除計劃的 12 項外另加了：
- DOM 結構不變式（階段 1-2 移除多餘 `</div>` 前後都必須成立）
- 原始碼守門（重複函式數量、div 平衡、`lastModifiedField` 是否設定）
- 快速調整按鈕連動
- 欄位 `readonly` / `disabled` 檢查（守住「不可喚起系統鍵盤」的設計理念）

**3. 發現一個計劃裡沒列到的 bug。**

> **載入頁面後「每月油錢」會被寫成 `0` 而不是留白。**
> 原因：存活的 `updateCalculations()` 裡有 `if (monthlyExpense === 0) expenseField.value = '0';`。
> 頁面載入時 `loadFuelPrices()` → `renderFuelPricesUI()` 會呼叫 `updateCalculations()`，
> 於是空欄位被填成 `0`，placeholder 永遠看不到。
> **已納入階段 1-1 一併處理**（測試代號：`載入後「每月油錢」應維持空白`）。

### ⚠️ 計劃書的一處錯誤已修正

原計劃在階段 1-2 與其他階段的驗收清單裡寫「跑 `node tests/版面樣式測試.js`」來驗證版面沒跑掉。**這是錯的** —— 該測試只讀 `pages/check.html`，完全不涵蓋加油頁，跑起來一定綠但什麼也沒驗到。

加油頁的結構驗證已改為內建在 `tests/加油頁功能測試.js` 的第 13、14 群組：
- **第 13 群組**：DOM 結構不變式（`.container`、`.price-checker-container`、`#calculatorModal` 都必須是 body 直接子元素；四個輸入欄位必須在 `.container` 內）—— 移除多餘 `</div>` 前後都要成立，這才是真正能抓到「元素跑到別的父層」的測試。
- **第 14 群組**：原始碼層級守門（div 開閉數量、重複函式）。

**後續各階段的驗收清單請以此為準**，`版面樣式測試.js` 只當作「確認沒影響到支票頁」的迴歸檢查。

### 技術注意事項

`pages/gas.html` 的 JS 目前是內嵌的，測試檔無法像計算頁那樣用 `<script src>` 正則抓外部檔。階段 0 的測試需先用**正則抓出內嵌 `<script>` 區塊內容**再 `eval`：

```js
const html = fs.readFileSync(R+'/pages/gas.html','utf8');
const inline = html.match(/<script>([\s\S]*?)<\/script>\s*<\/body>/)[1];
ev(inline.replace(/^let /gm,'var '));
```

階段 2 拆檔完成後，這段可以簡化成與計算頁一致的寫法（`<script src>` 抓取）—— 這也順便驗證了拆檔正確。

### 驗收（已全數通過 ✅）
- [x] `node tests/加油頁功能測試.js` 可執行不報錯
- [x] 結果為 **50 PASS / 0 FAIL / 12 XFAIL**，結束碼 0
- [x] 12 個 XFAIL 分別對應：階段 1-1（7 項）、1-2（1 項）、1-3（1 項）、1-4（1 項）、5-1（1 項）、新發現的載入寫 0 問題（1 項）
- [x] 連續執行兩次結果一致（無隨機性）
- [x] 其他頁面測試未受影響：計算頁 49、支票頁 56、版面樣式 30、共用鍵盤 70，全部 0 失敗
- [x] `tests/README.md` 已補上新測試與 XFAIL 說明

### Commit
```
test(gas): 新增加油頁功能測試，釘住 12 個已知問題為 XFAIL
```

> 階段 0 不需要執行 `bump-version.py` —— `tests/` 不會部署到網站，不影響手機取得的版本。

---

## 階段 1 — Bug 修復

**這是最重要的階段。** 改動小、風險低、但直接解決會給客戶錯誤數字的問題。**不動任何架構、不搬任何檔案。**

> 階段 1 的每一項都有對應的測試在盯。改動前先跑一次 `node tests/加油頁功能測試.js` 記下 12 個 XFAIL，改完再跑，看它們一個一個轉成 XPASS。

### 1-1 合併兩份 `updateCalculations`（核心）

**問題**：`pages/gas.html` 第 **1470 行**與第 **1612 行**各有一份 `function updateCalculations()`。JavaScript 後定義覆蓋前定義，所以**只有第 1612 行那份在運作**，第 1470 行那份是死碼。

兩份的差異：

| 行為 | 第 1470 行（死碼） | 第 1612 行（實際運作） |
|---|---|---|
| 油錢 → 油量 | ✅ | ✅ |
| **油量 → 油錢** | ✅ | ❌ **缺** |
| 依 `lastModifiedField` 判斷方向 | ✅ | ❌ 無視 |
| 折扣為 0 時清空結果區 | ✅ | ❌ **缺** |
| 去千分位處理 | `removeThousandsSeparator()` | 只 `replace(/,/g,'')` |
| 每次都強制回寫油錢欄位 | ❌ | ✅（會蓋掉使用者輸入） |

**造成的兩個實際故障**：

1. **「每月油量」欄位不能用。** 業務點油量欄位輸入 500 公升後：若油錢已有值 → 油量被立刻蓋回舊值；若油錢是空的 → 什麼都不算。
2. **折扣歸 0 時結果區不清空。** 按「-1」把折扣壓到 0.0，下方「折後油錢／每月節省／每年節省」還停在上一次的數字。

**做法**：

1. **刪除**第 1612–1648 行那份（後定義的）。
2. **保留**第 1470–1516 行那份（含 `lastModifiedField` 雙向推算與 `else` 清空分支）。
3. 在保留的那份裡，把去千分位改為統一呼叫 `removeThousandsSeparator()`（見 1-3）。
4. 檢查保留版本的「折後油錢」計算基準：目前它用 `Math.round(dieselPrice * monthlyVolume)` 重算油錢，而不是直接用欄位值。**確認這是刻意的**（避免使用者輸入的油錢與單價×油量不一致時，節省金額算錯），若是就在該行補上註解說明。

**改完的預期行為**：

| 使用者動作 | 結果 |
|---|---|
| 改「每月油錢」 | 反算「每月油量」 |
| 改「每月油量」 | 反算「每月油錢」 |
| 改「油品單價」 | 以油錢為基準重算油量 |
| 折扣設為 0.0 | 三個結果欄位清空 |
| 剛載入頁面（油錢空白） | 油錢**維持空白**，不可寫成 `0` |

> 最後一列是階段 0 新發現的問題：存活版本有 `if (monthlyExpense === 0) expenseField.value = '0';`，會把空欄位填成 0。保留版本沒有這段，所以刪掉存活版本就順便解決了 —— 但**要確認保留版本不會在其他路徑補寫 `'0'`**。

### 1-2 移除多餘的 `</div>`

**問題**：第 **937 行**有一個沒有對應開標籤的 `</div>`。body 內 36 個 `<div` 對 37 個 `</div>`。

瀏覽器會容錯，但實際 DOM 結構與原始碼看起來不同 —— `.container` 之後的元素掛在非預期的父層。**這正是「改版面時元素莫名跑位」的來源。**

**做法**：刪除第 937 行的 `</div>`。

**驗證**（重要，這是唯一會影響視覺的改動）：
```bash
node tests/加油頁功能測試.js    # 第 13 群組「DOM 結構不變式」必須全綠
```
第 13 群組的五項在刪除前後都必須是 PASS。若刪完有任何一項變 FAIL，代表那個 `</div>` 其實是有作用的，立刻還原。

並在手機實機確認：時間列 → 分隔線 → container → 油價表 的間距與改前一致。

### 1-3 移除重複的千分位函式

**問題**：兩個函式功能完全相同（同一條 regex）：
- `addThousandsSeparator()`（第 1536 行）
- `formatNumberWithCommas()`（第 1606 行）

**做法**：
1. 保留 `formatNumberWithCommas()`（與 `js/calc-ui.js`、`js/check-engine.js` 命名一致，未來拆檔可共用）。
2. 刪除 `addThousandsSeparator()`。
3. 全檔搜尋 `addThousandsSeparator` 並全數替換為 `formatNumberWithCommas`（1-1 完成後應有 4 處）。

### 1-4 清理 `lastModifiedField` 的相關註解

1-1 修完後 `lastModifiedField` 會真正被使用（不再是死碼）。檢查四處賦值點是否都正確：
- 第 1303 行 `setDieselPrice()` → `'dieselPrice'`
- 第 1458 行 `confirmCalculation()` → `currentInput`
- 第 1687 行 `adjustDiscountAmount()` → `'discountAmount'`
- 第 1714 行 `setDiscountAmount()` → `'discountAmount'`

**缺漏**：`adjustMonthlyExpense()`（第 1567 行）**沒有**設定 `lastModifiedField`。1-1 修完後這會導致「按 +10 萬調整油錢」時方向判斷失準。**必須補上** `lastModifiedField = 'monthlyExpense';`。

> 這是 1-1 修好後才會浮現的連帶問題，務必一起處理，否則會出現「修完反而更奇怪」的狀況。

### 階段 1 驗收清單（已完成 ✅ 版本 4.35 → 4.36）

- [x] `node tests/加油頁功能測試.js` → **11 個 XFAIL 全部轉成 XPASS**，0 FAIL
- [x] 已把那 11 行 `xt(...)` 改成 `t(...)`，並補了 7 項新測試
- [x] 最終結果：**72 PASS / 0 FAIL / 1 XFAIL**（保留的 XFAIL 是階段 5-1 的離線註記）
- [x] 全套迴歸 0 失敗：計算頁 49、支票頁 56、支票開票 81、支票進度 80、版面樣式 30、共用鍵盤 70、大寫金額 44、invoice/verify
- [x] `node --check` 語法檢查通過
- [x] 全檔搜尋 `function updateCalculations` → 只剩 **1** 處
- [x] 全檔搜尋 `addThousandsSeparator` → **0** 處
- [x] `<div` 與 `</div>` 各 36 個，數量相等
- [x] `python3 scripts/bump-version.py` → 4.36
- [ ] **手機實機待 Ryan 確認**：輸入油量 500 → 油錢自動出現
- [ ] **手機實機待 Ryan 確認**：折扣按到 0.0 → 下方三格清空
- [ ] **手機實機待 Ryan 確認**：按「+10」調整油錢 → 油量正確跟著變
- [ ] **手機實機待 Ryan 確認**：版面與改前目視一致（尤其時間列與油價表的間距）

### 階段 1 補充的測試（共 7 項，原計劃沒有）

修完之後才想到幾個「雙向推算最容易壞」的情境，都補進測試了：

1. **連續切換輸入方向三次**（油量 → 油錢 → 油量）。單向計算時代不會壞，但雙向推算一旦方向判斷有誤就會在這裡現形。
2. **先輸入油量、再按油錢調整鍵**。這是 1-4 真正要防的情境 —— 若 `adjustMonthlyExpense()` 沒設 `lastModifiedField`，剛才 +10 萬的結果會被反算蓋掉，而畫面上完全看不出異常。
3. **清除後再設單價，結果區必須仍為空**。
4. **五個輸入入口都必須設定 `lastModifiedField`** 的原始碼守門（`setDieselPrice`、`confirmCalculation`、`adjustMonthlyExpense`、`adjustDiscountAmount`、`setDiscountAmount`）。這是最有價值的一項：以後不論誰新增輸入入口，漏設就會被測試抓到。

### 實際改動摘要

`pages/gas.html`：**+55 / −71 行**（淨減 16 行，1736 → 1720 行）

| 項目 | 改動 |
|---|---|
| 1-1 | 刪除後定義的 `updateCalculations()`（含把空欄位寫成 `0` 的那段），保留雙向推算版並補上完整的歷史成因註解 |
| 1-2 | 刪除多餘 `</div>`，順手移除已無對應元素的「主要內容容器」註解 |
| 1-3 | 刪除 `addThousandsSeparator()`，1 處呼叫改為 `formatNumberWithCommas()` |
| 1-4 | `adjustMonthlyExpense()` 補上 `lastModifiedField = 'monthlyExpense'`，並註明萬元單位 |

**另外發現**：`validateMonthlyExpense()` 因 1-1 的刪除而變成孤兒（零呼叫者）。**沒有立刻刪**，以維持階段 1 的改動範圍只限 bug 修復，已在原始碼加註並登記到階段 6-4。

### Commit
```
fix(gas): 修正 updateCalculations 重複定義導致的油量反算失效與折扣歸零不清空

- 刪除後定義的重複 updateCalculations()，保留含雙向推算與 else 清空的版本
- 順帶解決載入時把空白油錢欄位寫成 0 的問題
- 補上 adjustMonthlyExpense() 漏設的 lastModifiedField（否則雙向推算會反向蓋值）
- 移除多餘的 </div> 與重複的 addThousandsSeparator()
- 加油頁測試 72 項全綠，11 個 XFAIL 轉正
```

---

## 階段 2 — 拆檔（CSS / JS 外部化）

**本階段是純搬移，一行邏輯都不改。** 目的是讓加油頁與其他三頁架構一致。

### 2-1 抽出 `css/gas.css`

**做法**：
1. 建立 `css/gas.css`。
2. 把 `pages/gas.html` 第 **170–923 行**（`<style>` 內容，不含 style 標籤本身）整段搬進去。
3. 在 `<head>` 加入 `<link rel="stylesheet" href="../css/gas.css">`。
4. 刪除原 `<style>` 區塊。

**這階段先不要合併重複樣式。** 加油頁與 `calculator.css` 有 8 個同名選擇器（`#timeDisplay`、`.sticky-header`、`.divider-flow`、`.time-display-wrapper`、`.time-label`、`.input-button-group`、`.calculator-display`、`.calculator-buttons`）與 31 個 CSS 變數重複，但**合併會改變視覺**（兩邊的值不一定相同）。留到階段 5 逐一比對，本階段只求「搬移後長得完全一樣」。

### 2-2 抽出 `js/gas-engine.js`

**做法**：
1. 建立 `js/gas-engine.js`。
2. 把 `pages/gas.html` 底部 `<script>`（階段 1 修完後約 1151–1700 行）整段搬進去。
3. 在 `</body>` 前加入 `<script src="../js/gas-engine.js"></script>`。
4. 刪除原內嵌 `<script>` 區塊。

**必須注意的兩點**：

- **`window.onload` 改成 `DOMContentLoaded`。** 目前用 `window.onload`（第 1167 行），要等圖片與字型全部載完才綁定欄位點擊事件，首屏有一段時間點欄位沒反應。外部檔案載入時序改變後這問題會更明顯。改為：
  ```js
  document.addEventListener('DOMContentLoaded', function() { ... });
  ```
- **`onclick` 內嵌屬性暫時保留。** HTML 裡有 26 處 `onclick="..."`，全部依賴函式掛在全域。搬到外部檔案後仍是全域函式，行為不變。**這階段不要改成事件委派**，那是階段 5 的事。

### 2-3 更新 `sw.js` 快取清單

**做法**：在 `sw.js` 的預快取陣列補上兩個新檔（緊接 `./pages/gas.html` 之後，維持既有註解風格）：
```js
'./css/gas.css',            // 加油頁專用樣式（原內嵌於 gas.html）
'./js/gas-engine.js',       // 加油頁計算引擎（原內嵌於 gas.html）
```

> 漏這一步的後果：離線時加油頁會變成沒有樣式的裸 HTML，且完全不能計算。**這是本階段最容易漏、後果最嚴重的一步。**

### 2-4 簡化測試檔的抓取方式

拆檔完成後，把 `tests/加油頁功能測試.js` 開頭的「正則抓內嵌 script」改成與計算頁一致的 `<script src>` 抓取。**能正常跑通，就等於驗證了拆檔正確。**

### 階段 2 驗收清單（已完成 ✅ 版本 4.36 → 4.37）

- [x] `node tests/加油頁功能測試.js` → **87 PASS / 0 FAIL / 1 XFAIL**（已改用 src 抓取）
- [x] 全套迴歸 0 失敗（9 支測試）
- [x] `pages/gas.html` 1727 → **399 行**（calculator 350 / check 227 / invoice 269，同級）
- [x] `pages/gas.html` 內已無 `<style>`、內嵌 `<script>` 只剩 head 的 GA4 區塊
- [x] `sw.js` 已含 `gas.css` 與 `gas-engine.js`，且清單 30 個資源全部確認實際存在
- [x] `node --check` 語法通過、CSS 大括號平衡
- [x] `python3 scripts/bump-version.py` → 4.37
- [ ] **手機實機待 Ryan 確認**：開飛航模式後仍能開啟加油頁並完成一次完整試算
- [ ] **手機實機待 Ryan 確認**：版面與改前目視完全一致

### 搬移正確性的驗證方式（重點）

拆檔最怕的是「搬的時候掉了幾行沒人發現」。所以除了測試，還做了兩項機械比對：

**CSS** — 把舊的內嵌 `<style>` 與新的 `css/gas.css` 都去掉註解與空白後拆成宣告集合比對：

```
宣告數   舊 415 / 新 415   ✅ 一致
選擇器   舊  80 / 新  80   ✅ 完全相同，無遺失也無多出
```

**JS** — 去除註解與所有空白後做字元級 diff，差異**只有**下列四項，全部是階段 1、2 刻意的改動：

| 差異 | 來源 |
|---|---|
| `window.onload = function` → `function initGasPage` + `readyState` 判斷 | 階段 2-2 |
| `addThousandsSeparator` → `formatNumberWithCommas`（6 處） | 階段 1-3 |
| 新增 `lastModifiedField = 'monthlyExpense';` | 階段 1-4 |
| 刪除第二份 `updateCalculations` | 階段 1-1 |

**沒有任何非預期的差異** —— 搬移本身沒有改動一行程式碼。

### 實際檔案結構（四頁終於一致）

| 頁面 | HTML 行數 | CSS | JS |
|---|---|---|---|
| calculator | 350 | calculator.css | frame-guard, calc-engine, common-keypad, calc-storage, calc-ui, common-modals |
| check | 227 | calculator.css, check.css | frame-guard, common-keypad, common-modals, check-engine |
| invoice | 269 | invoice.css | frame-guard, taxid-lookup, invoice-engine |
| **gas** | **399** | **gas.css** | **frame-guard, gas-engine** |

新增檔案：`css/gas.css`（775 行）、`js/gas-engine.js`（603 行）

### 階段 2 的兩個額外處理

**1. `window.onload` 改法比原計劃更保險。** 原計劃只寫「改成 `DOMContentLoaded`」，但那樣還有一個漏洞：外部腳本若因快取而載入得比 `DOMContentLoaded` 更晚，事件早就過了，初始化永遠不會執行，**整頁會完全不能操作**。所以改成：

```js
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initGasPage);
} else {
    initGasPage();          // 腳本晚到，直接跑
}
```

**2. 測試新增第 16 群組「拆檔守門」（16 項）。** 包含 `sw.js` 是否登記了兩個新檔、其他頁面的資源有沒有被手滑刪掉、初始化時機是否正確。以後改 `sw.js` 漏登記會直接被測試抓到，不必等到手機離線才發現。

### 順帶確認的事

`data/fuel-prices.json` **刻意沒有**放進預快取清單（`sw.js` 第 29 行已有註解說明），它走「網路優先 + 執行時快取」策略：有網路拿最新，沒網路用上次抓到的。所以離線時並不會掉到硬編碼的 28.8/28.6 fallback ——**那個 fallback 只有在「從未成功抓過油價 + 目前離線」時才會出現**。階段 5-1 的優先度可以下調。

### Commit
```
refactor(gas): 將內嵌 CSS 與 JS 抽出為 css/gas.css 與 js/gas-engine.js

- gas.html 1727 → 399 行，四個功能頁架構終於一致
- window.onload 改為 DOMContentLoaded，並處理腳本晚於該事件載入的情況
- sw.js 補上兩個新檔的預快取（漏掉會導致離線時加油頁完全失效）
- 測試改用 script src 抓取，與計算頁一致；新增 16 項拆檔守門
- 機械比對確認搬移無遺漏：CSS 宣告 415/415、選擇器 80/80，JS 差異僅階段 1、2 的刻意改動
```

---

## 階段 3 — GA4 瘦身

**問題**：加油頁的 GA4 區塊（第 16–168 行）保留了其他三頁都已清掉的舊追蹤，而且會嚴重污染數據。

### 3-1 移除「全按鈕無條件追蹤」（優先）

第 84–92 行用 `document.querySelectorAll('button')` 給**所有** button 綁 click 追蹤。加油頁的計算機有 **22 個按鍵**，業務算一筆按十幾下就送出十幾個 `button_click` 事件。

**後果**：GA4 報表被計算機按鍵淹沒，看不出真正有意義的操作；同時白耗業務的手機流量。

**做法**：改為只追蹤**有商業意義**的操作，用明確的白名單：
- 點「中」/「塑」快速單價按鈕
- 點油價對照表帶入單價
- 完成一次有效試算（三個結果欄位都有值時）
- 按「清除」

計算機按鍵、退格、確認/取消**一律不追蹤**。

### 3-2 移除三個舊追蹤（其他頁面已無）

| 追蹤 | 行號 | 問題 |
|---|---|---|
| 滾動深度 | 104–122 | **未 throttle 的 scroll listener**，滾動時每一幀都在算 `document.body.scrollHeight`（強制 reflow），是明顯的卡頓來源 |
| 長按偵測 | 139–152 | 每次 touchstart 都開 500ms timer，加油頁沒有任何長按功能，純浪費 |
| beforeunload | 125–130 | iframe 架構下觸發時機不可靠（index.html 用 `display:none` 切頁不卸載），數據無意義 |

**做法**：三段全部刪除。刪掉後 `maxScrollDepth`、`scrollTracked`、`touchTimer` 三個變數也一併移除。

### 3-3 停掉背景頁的時鐘

**問題**：第 1189 行 `setInterval(updateCurrentDate, 1000)` 每秒重繪整串時間字串。index.html 是用 `display:none` 切換 iframe（**不卸載**），所以四個頁面的時鐘 timer 會同時在背景空轉。

**做法**：加入 `visibilitychange` / iframe 顯示狀態判斷，頁面不可見時 `clearInterval`，重新可見時先立刻更新一次再重啟 interval。

> 注意：iframe 用 `display:none` 隱藏時，`document.hidden` **不一定**會變 true（取決於瀏覽器實作）。若實測無效，改用 index.html 切頁時 `postMessage` 通知 iframe 暫停 —— 頁面本來就已有 `iframe_loaded` 的 postMessage 機制可沿用。

### 3-4 考慮把 GA4 區塊也抽成共用檔

四個頁面的 GA4 區塊是複製貼上的（`getPageTitle` 的頁面對應表四份、`trackEvent` 四份）。**本階段先不做**，只在計劃裡記錄：清完加油頁的雜訊後，四頁的 GA4 區塊差異會縮小到只剩頁面名稱，屆時抽成 `js/common-analytics.js` 的成本最低。

### 階段 3 驗收清單

- [ ] `node tests/加油頁功能測試.js` → 12 項全 PASS
- [ ] 全檔搜尋 `scroll_depth`、`long_press`、`beforeunload` → **0** 處
- [ ] 全檔搜尋 `querySelectorAll('button')` → **0** 處
- [ ] 瀏覽器 Console：按 10 下計算機按鍵，**不應**出現 `GA4 Event` 訊息
- [ ] 瀏覽器 Console：點「中」按鈕，**應**出現一筆事件
- [ ] 手機實機：切到其他頁再切回來，時間顯示正確（沒有停在舊時間）
- [ ] `python3 scripts/bump-version.py`

### Commit
```
perf(gas): 移除全按鈕追蹤與滾動/長按舊追蹤，背景時停止時鐘 timer
```

---

## 階段 5 — UX 調整（需逐項確認）

**這階段每一項都會改變業務的操作習慣，建議逐項單獨 commit，並在改前確認。** 依「爭議性」由低到高排列。

### 5-1 離線 fallback 不要騙人（無爭議，建議直接做）

**問題**：`data/fuel-prices.json` 讀取失敗時（第 1210–1216 行），fallback 是硬編碼的 28.8 / 28.6，而 `updatedDateStr` 顯示 **「即時數據」**。業務會誤以為看到的是最新油價，拿去跟客戶談。

**做法**：
1. `updatedDateStr` 改為明確標示，例如 `'離線預設值（非最新）'`。
2. 表格下方註記加上視覺區隔（改用警示色）。
3. 考慮在 fallback 值旁加註最後已知的日期。

### 5-2 快速按鈕加上單位標示（無爭議）

**問題**：每月油錢按鈕標示 `-100 -10 -1 +1 +10 +100`，但實際 delta 是 `1000000`（100 萬）。**單位是「萬元」但畫面上完全看不出來。**

**做法**：在按鈕組加一個極小的單位標籤（例如靠左的 `萬`），或在 `title` 屬性補完整說明。**不改按鈕文字本身**（業務已有肌肉記憶）。

### 5-3 折扣按鈕的雙重語意（需確認）

**問題**：折扣按鈕一排 `-1 | 4 5 6 7 8 9 | +1`，中間六顆是「**設定**為 0.4～0.9」，兩端是「**加減** 0.1」。視覺上完全相同，兩種語意混在一排。

老手靠肌肉記憶沒問題，新人必踩。

**選項**：
- (A) 視覺區隔：±0.1 兩顆用不同底色，中間六顆維持現狀。**改動最小、不影響習慣，建議採此案。**
- (B) 加上小標籤區分兩區。
- (C) 不改。

> **這項需要 Ryan 確認採哪個方案**，因為只有實際使用的業務知道誤觸頻率。

### 5-4 統一重複的 CSS 變數與選擇器（需仔細比對）

階段 2 刻意沒做的事。`css/gas.css` 與 `css/calculator.css` 有 8 個同名選擇器與 31 個重複變數。

**做法**：**逐一比對數值**，只合併值完全相同的；值不同的保留在 `gas.css` 覆寫並加註解說明為何不同。**不要為了統一而改變視覺。**

建議做法是先產出比對表，確認哪些能合、哪些不能，再動手。

### 5-5 清理殘留的「超級柴油」註解（無爭議）

JS 註解多處寫「超級柴油」，但欄位已改名「油品單價」，且油價表可帶入 92/95/98 汽油。註解與實際功能不符，會誤導未來的修改。全檔統一改為「油品單價」。

### 5-6 加油頁存檔功能（新功能，另案評估）

計算頁有 `calc-storage.js`、支票頁有存檔，**加油頁按「清除」就全沒了**。

這是新增功能不是優化，**不納入本次計劃**，僅記錄為後續待評估項目。若要做，可沿用 `calc-storage.js` 的模式。

### 階段 5 驗收清單

- [ ] 每一子項單獨 commit
- [ ] `node tests/加油頁功能測試.js` → 全 PASS
- [ ] `node tests/版面樣式測試.js` → 全 PASS
- [ ] 手機實機逐項確認視覺與操作
- [ ] `python3 scripts/bump-version.py`

---

## 階段 6 — 死碼清理

**風險最低、可以隨時做。** 放在階段 5 之後是因為階段 5 可能會用到其中一些變數。

### 6-1 未使用的 CSS class（2 個）
```
.button-group-container
.unit-container
```

### 6-2 未使用的 CSS 變數（10 個）
```
--button-blue      --error-color      --font-size-button
--font-size-input  --font-size-label  --font-size-title
--radius-lg        --shadow-lg        --shadow-md
--shadow-sm        --surface-color
```

> **先確認階段 5-4 不需要這些變數**再刪。特別是 `--font-size-*` 系列 —— 如果階段 5-4 決定與 `calculator.css` 對齊字級，這些反而應該啟用而不是刪除。

### 6-3 `removeThousandsSeparator` 的使用範圍
階段 1 修完後這個函式應在 `openCalculator` 與 `updateCalculations` 兩處使用。若實際只有一處，考慮內聯掉。

### 6-4 `validateMonthlyExpense()` 已成孤兒（階段 1 產生）
階段 1-1 刪掉重複的 `updateCalculations()` 之後，這支函式**失去所有呼叫者**。原本只有被刪掉的那份在用。

沒有在階段 1 一併刪除，是為了讓該階段的改動範圍只限於 bug 修復。原始碼已加註說明。刪除時順便確認保留版的 `updateCalculations()` 是否應該改用它做油錢驗證（目前用的是 `parseInt(removeThousandsSeparator(...)) || 0`，行為等價）。

### 階段 6 驗收清單
- [ ] `node tests/加油頁功能測試.js` → 全 PASS
- [ ] `node tests/版面樣式測試.js` → 全 PASS
- [ ] 手機實機目視無異常
- [ ] `python3 scripts/bump-version.py`

### Commit
```
chore(gas): 清理未使用的 CSS class 與變數
```

---

## 階段 4 — 併入共用鍵盤（最後執行，風險最高）

> **依決策排在最後。前面所有階段穩定後再評估是否執行。**

### 現況

加油頁自己重寫了一整套計算機（`openCalculator` / `calcInput` / `calcEquals` / `calcClear` / `calcBackspace` / `confirmCalculation` / `closeCalculator`），而 `js/common-keypad.js` 已有同名同功能的實作。

**好消息：介面接縫是乾淨的。** 共用鍵盤的分工是：

- `common-modals.js` → 注入鍵盤 HTML
- `common-keypad.js` → 提供 `openCalculator(targetId, title)` 與四則運算
- **各頁自行實作 `submitCalculatorValue()`** 處理各自的欄位驗證規則

計算頁（`calc-ui.js:653`）與支票頁（`check-engine.js:396`）都是這個模式。加油頁只需在 `gas-engine.js` 實作自己的 `submitCalculatorValue()`。

### 要做的事

1. `pages/gas.html` 載入 `common-modals.js` + `common-keypad.js`，並呼叫 `initCommonModals()`。
2. `gas.html` 需一併載入 `css/calculator.css`（鍵盤樣式在那裡），或把鍵盤樣式複製進 `gas.css`。
3. 刪除 `gas.html` 內的 `<div id="calculatorModal">` 整段（第 1107–1149 行）與自有鍵盤的 7 個函式。
4. 在 `gas-engine.js` 實作 `submitCalculatorValue()`，搬入原 `confirmCalculation()` 的四組欄位規則：

| 欄位 | 規則 |
|---|---|
| `dieselPrice` | ≥ 0，保留 1 位小數 |
| `discountAmount` | 夾在 0～5，保留 1 位小數 |
| `monthlyExpense` | 正整數 + 千分位 |
| `monthlyVolume` | 正整數 + 千分位 |

5. 更新 `sw.js`（若新增 `calculator.css` 依賴）。

### 必須事先接受的三個代價

1. **會失去「00」鍵。** 共用鍵盤沒有這顆。輸入 10000 要多按兩下。
2. **鍵盤外觀會變。** 共用鍵盤是「阿拉伯數字 + 國字大寫」雙層按鍵，排列也與加油頁現有的 `C/←/÷/×` 佈局不同。業務需重新適應肌肉記憶。
3. **`KEYPAD_OPTIONS` 是頁面層級不是欄位層級。** 加油頁四個欄位中兩個要小數、兩個是整數，無法用 `KEYPAD_OPTIONS.decimal` 分別控制 —— 小數點鍵必須全開，靠 `submitCalculatorValue()` 事後修正。這與支票頁「三欄位全整數所以關掉小數點」的乾淨做法不同。

### 判斷建議

**這階段的收益是「維護時只需改一處」，代價是「業務要重新習慣鍵盤」。** 若加油頁鍵盤未來預期很少變動，維持自有鍵盤（不執行本階段）是完全合理的選擇。

**建議在階段 1–3、5、6 都完成後再重新評估**：屆時 `gas-engine.js` 已獨立成檔、程式碼清楚，維護成本可能已經降到不值得承擔 UX 風險。

### 階段 4 驗收清單（若執行）
- [ ] `node tests/加油頁功能測試.js` → 全 PASS
- [ ] `node tests/共用鍵盤測試.js` → 全 PASS
- [ ] `node tests/鍵盤行為對照測試.js` → 全 PASS（此檔專門比對各頁鍵盤一致性）
- [ ] 手機實機：四個欄位各測一次輸入、小數處理、上下限
- [ ] 手機實機：**確認鍵盤彈出時畫面沒有位移或遮擋**（設計理念的硬要求）
- [ ] `python3 scripts/bump-version.py`

### Commit
```
refactor(gas): 改用 common-keypad 共用鍵盤，移除自有計算機實作
```

---

## 四、每階段通用檢查（貼在手邊備用）

不論做哪個階段，收工前都跑一遍：

```bash
# 1. 加油頁功能
node tests/加油頁功能測試.js

# 2. 版面沒跑掉
node tests/版面樣式測試.js

# 3. 確認沒影響其他頁面
node tests/計算頁功能測試.js
node tests/支票頁功能測試.js

# 4. 版本遞增（否則手機拿不到新版）
python3 scripts/bump-version.py
```

**手機實機必測三項**（設計理念的底線）：
1. 點任何輸入欄位，**系統鍵盤不會跳出來**
2. 自製鍵盤彈出時，**畫面不位移、不遮擋**要看的數字
3. 固定標題列在滾動時**維持固定**

---

## 五、建議的執行節奏

| 順序 | 階段 | 建議 |
|---|---|---|
| 1 | 階段 0 | 一定要先做，後面全靠它把關 |
| 2 | 階段 1 | **最高優先**。改動小、直接解決客戶會看到的錯誤 |
| 3 | 階段 2 | 一次性收益最大，但務必記得改 `sw.js` |
| 4 | 階段 3 | 純刪除，很安全 |
| 5 | 階段 5 | 逐項確認，5-3 需要 Ryan 決定方案 |
| 6 | 階段 6 | 收尾 |
| 7 | 階段 4 | 前面都穩了再重新評估要不要做 |

**如果時間有限只能做一件事：做階段 0 + 階段 1。** 那是唯一會影響業務給客戶錯誤數字的部分。

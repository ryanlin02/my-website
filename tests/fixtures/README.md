# 改版前的原始碼快照

這兩個檔案是 **B1 抽出共用鍵盤模組之前** 的原版（取自 commit 6eb5cba^）。

保留它們的用途只有一個：讓 `tests/鍵盤行為對照測試.js` 可以永遠拿
「改版前的兩份鍵盤實作」和「改版後的 js/common-keypad.js」做行為比對，
不必依賴 git 歷史。

**這兩個檔案不會被網站載入，也不應該再修改。**
它們是一份凍結的基準線，證明重構沒有改變任何行為。

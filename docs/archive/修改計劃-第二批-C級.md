# 修改計劃：第二批（C 級 — 更新與快取）

擬定日期：2026/07/25
狀態：**待你確認，尚未動任何程式碼**

---

## 已確認的三個決定

| 項目 | 決定 |
|---|---|
| 版本號 | **4.24 起跳，每次更新 +0.01**，由 GitHub Actions 自動遞增 |
| 更新機制 | **靜默更新 + 小提示條**，可點擊立即套用 |
| 油價歷史 | **這次不處理**，只修快取問題；圖表要做時再直接查詢公開歷史資料 |

---

## 影響範圍

這批比第一批廣，而且動到的是**整個 App 的更新機制**，所以風險等級不同，請先看最後的「急救方法」一節。

| 檔案 | 改動 | 風險 |
|---|---|---|
| `sw.js` | 快取清單、安裝方式、油價策略、移除強制接管 | **中** |
| `index.html` | 更新提示條 + 移除自動重整與詢問框 | **中** |
| `pages/gas.html` | 油價抓取拿掉時間戳（1 行） | 低 |
| `js/common-modals.js` | 頁尾顯示真實版本號 | 低 |
| `manifest.json` | 版本號（由 Action 自動更新） | 低 |
| `.github/workflows/version-bump.yml` | **新增檔案** | 低 |

**不會動到**：`js/calc-*.js`、`js/check-engine.js`、`js/invoice-engine.js`、`css/`、`pages/calculator.html`、`pages/check.html`、`pages/invoice.html`。

---

## 改動一：版本號自動化（C-1）

### 版本號規則

```
4.24 → 4.25 → 4.26 → ... → 4.99 → 5.00 → 5.01 ...
```

用整數運算處理（424 + 1 = 425 → 顯示 4.25），不會有小數相加的誤差。

### 自動遞增機制

新增 `.github/workflows/version-bump.yml`，跟你現有的油價自動更新是同一套做法：

```
你推上 GitHub
  → Action 讀出 sw.js 目前版本 4.24
  → 改寫成 4.25（同時更新 manifest.json）
  → 自動 commit，訊息帶 [skip ci]（避免觸發自己造成無限迴圈）
```

**只有這些檔案變動時才會遞增**：`js/`、`css/`、`pages/`、`instructions/`、`about/`、`index.html`、`sw.js`、`manifest.json`

**不會遞增的**：`data/`（油價自動更新）、`*.md`（說明文件）。因為油價走網路優先策略，不需要靠版本號更新。

### 「確實更新」的三層保障

**第一層：版本號一定會變** — 自動化，不會再忘。

**第二層：安裝時強制抓最新檔案。**
目前是漏的。GitHub Pages 會對每個檔案回一個「10 分鐘內可直接用舊的」指示，所以就算 Service Worker 重裝，也可能從瀏覽器暫存拿到舊檔。這次在抓取時明確標註「不要用暫存」。

順帶修一個相關問題：現在的預快取是「一次抓 13 個檔案，任何一個失敗就整批失敗」，而失敗還被程式吞掉不報錯 —— 等於離線功能可能早就是壞的而你不會知道。改成逐一抓取，單一檔案失敗不影響其他，並記錄是哪個檔案失敗。

**第三層：你看得見。**
目前三個版本號互相矛盾（manifest 1.0.1 / sw v1.0.3 / 頁尾 v2.5.0），全都是假的。

改成頁尾**直接向 Service Worker 詢問「你現在跑的是哪一版」**並顯示出來。這是手機上真正在運作的版本，不是寫死的字串。

```
重車貸款業務工具箱 • v4.25
```

**你唯一需要做的動作**：打開 App 看一眼頁尾，跟 GitHub 上的數字比對。一樣就是最新版。

（註：加油頁沒有載入頁尾模組，所以版本號會出現在計算／支票／發票三頁。）

---

## 改動二：補齊離線快取清單（C-2）

目前預快取 13 項，補上漏掉的 9 個：

```
js/check-engine.js          ← 沒有這個，離線時支票頁開不起來
js/invoice-engine.js        ← 沒有這個，離線時發票頁開不起來
js/common-modals.js         ← 三頁共用的彈窗與小鍵盤
instructions/calculator_instruction.html
instructions/check_instruction.html
instructions/invoice_instruction.html
instructions/gas_instruction.html
about/privacy-policy.html
about/terms-of-service.html
```

一次性多下載約 200KB。

**`data/fuel-prices.json` 刻意不放進預快取** —— 它需要的是最新資料而不是離線備份，走下面的網路優先策略。

---

## 改動三：油價快取不再撐爆（C-3）

**現在的問題**：每次抓油價都加時間戳當網址（`?v=1753...`），Service Worker 把每一次都當成不同檔案各存一份，用久了會累積上千筆一模一樣的資料。

**改法（兩處）**：

1. `pages/gas.html`：拿掉時間戳，改用標準的「不要用暫存」標註
2. `sw.js`：`data/` 底下改成「網路優先、失敗時用快取」

**結果**：
- 永遠只有 **1 筆**快取，每次抓到新的就覆蓋 → 不會膨脹
- 有網路 → 拿到最新油價
- 沒網路 → 用上次抓到的油價，功能不中斷

**不需要「超過多久刪除」的機制** —— 只有一筆一直覆蓋，加時間過期反而多一個會出錯的地方。

---

## 改動四：更新機制整併（C-4）

### 現在的狀況（三套打架）

1. `sw.js` 安裝完就無條件強制接管
2. `index.html` 偵測到接管 → **自動整頁重新載入**
3. `index.html` 同時又跳 `confirm('發現新版本，是否立即更新？')`

導致「按了取消結果還是重整了」，而且有機率在你正在跟客戶輸入數字時發生。

### 改成

**只有一套流程，而且絕不會自己重整：**

```
偵測到新版本
  → 畫面下方出現一條小提示：「已有新版本 v4.25　[立即更新]」
  → 不擋住任何操作，不影響輸入

  你不理它  → 下次完全關掉 App 再開，自動生效
  你點一下  → 立刻套用並重新載入
```

**關鍵設計**：整頁重新載入**只會在你主動點擊之後**發生。程式裡會用一個明確的旗標記錄「這次是使用者要求的」，沒有這個旗標就絕不重整。

### 「完全關掉 App」是什麼意思

這點容易誤會：是**從多工清單把 App 滑掉**，不是按 Home 鍵切到背景。按 Home 只是切到背景，App 還活著，新版不會生效。

---

## 改完之後你會看到的變化

**不會變的：**
- 所有畫面排版、功能、計算結果 — 完全不動
- 第一批修好的利率計算 — 不受影響
- 已存的歷史記錄 — 完全保留

**會變的：**
1. 頁尾版本號從假的 `v2.5.0` 變成真實的 `v4.25`（會跟著自動遞增）
2. 有新版時畫面下方出現一條小提示，可點可不點
3. 不會再跳「發現新版本，是否立即更新？」的詢問框
4. 不會再在使用中突然整頁重整
5. 離線時支票頁、發票頁、說明頁都打得開了
6. 油價快取不再無限累積

---

## 驗證方式

1. **Service Worker 語法與邏輯檢查**：在 Node 環境模擬 install / activate / fetch 三個事件，確認快取清單完整、油價走網路優先、其他資源走快取優先
2. **版本遞增測試**：模擬 4.24 → 4.25 → ... → 4.99 → 5.00 的進位，確認不會出現 4.250000001 這類浮點誤差
3. **不會自己重整**：檢查所有會呼叫 `reload()` 的路徑，確認每一條都必須先經過使用者點擊
4. **離線清單完整性**：逐一確認清單裡的每個檔案都真實存在（避免有檔案打錯字導致整批失敗）
5. **不回歸**：用檔案指紋確認計算／支票／發票三頁與各 JS 引擎未被更動
6. **公司名稱掃描**

**但有一件事我在電腦上驗不了**：Service Worker 的實際更新流程需要真實瀏覽器多次開關才能完整驗證。所以下面這節很重要。

---

## ⚠️ 急救方法（請務必看這節）

Service Worker 是整個網站風險最高的部分。原因是：**它會常駐在使用者手機裡，如果推上去一個壞掉的版本，它可能會一直把壞版本餵給使用者**，即使你已經在 GitHub 上修好了。

所以這批改動我建議這樣進行：

**第一步**：我改完後，你先在**電腦的 Chrome** 上測（電腦上清除很容易，手機很麻煩）
**第二步**：電腦上確認正常後，再用**你自己的手機**測
**第三步**：都沒問題才通知其他業務同事更新

### 萬一手機上真的卡住了

按嚴重度排列的三個方法：

1. **關掉 App 再開兩次** — 大部分情況這樣就好
2. **移除主畫面圖示，重新用瀏覽器開網址再加入主畫面一次**
3. **手機瀏覽器設定 → 清除該網站資料** — 這一步會清掉快取，但**歷史記錄也會一併清掉**，請先確認同事沒有重要的試算紀錄

第 3 點是我特別要提醒的：你們的試算歷史存在瀏覽器裡，清除網站資料會一起清掉。所以測試階段請不要找正在跑重要案子的同事當白老鼠。

### 保險做法

這批我會獨立成一次 commit。如果推上去發現不對，可以立刻還原並推上「還原版」，Service Worker 會在下次檢查時拿到還原後的版本。

---

## 這批不會處理的

- B 級：版面 15px 偏移、iOS 底部被切、加油頁縮放不一致
- D 級：主畫面四個捷徑全開到計算頁、manifest 清理
- E 級：小鍵盤三份合一、加油頁 1720 行拆檔
- F 級：月曆翻頁 bug、備註特殊符號

（D 級的 manifest 清理跟這批有點相關，但我刻意分開 —— 這批已經動到更新機制了，不想同時改太多。）

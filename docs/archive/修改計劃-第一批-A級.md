# 修改計劃：第一批（A 級 — 會算錯數字的問題）

擬定日期：2026/07/25
狀態：**待你確認，尚未動任何程式碼**

---

## 已確認的三個決定

| 項目 | 決定 |
|---|---|
| 金額上限 | 本金／期繳／推廣費 **三個統一 99,999,999** |
| 利率硬上限 | **100%**；超過 20% 跳警告但可繼續計算 |
| 警告方式 | **紅字常駐 + 跨過門檻時跳一次提示** |

---

## 影響範圍

**只動計算頁。支票頁、發票頁、加油頁完全不碰。**

要修改的檔案共 3 個：

| 檔案 | 改動性質 |
|---|---|
| `js/calc-engine.js` | 換掉 `RATE()` 演算法、新增上限設定區 |
| `js/calc-ui.js` | 調整數值守衛、加入警告觸發邏輯 |
| `sw.js` | 只改版本號 1 行（不改的話手機收不到更新，見下方說明） |

HTML 完全不動，畫面排版不會有任何位移。

---

## 改動一：把所有上限集中到一個地方

在 `js/calc-engine.js` 最上方新增一段設定區：

```js
const LIMITS = {
    MAX_AMOUNT:  99999999,  // 本金／期繳／推廣費的共用上限
    MAX_PERIOD:  999,       // 期數上限
    MAX_RATE:    100,       // 利率硬上限（%）
    RATE_WARN:   20,        // 利率警告門檻（%）
    RATIO_WARN:  5,         // 佣金比例警告門檻（%）
    MAX_FUNDING_COST: 20    // 資金成本上限（%），維持現狀
};
```

**為什麼這樣做**：目前這些數字散在程式的 8 個不同位置，這也是「本金 10 億但期繳只能 99 萬」這種不一致的來源。集中之後，以後你要調整任何一個門檻，就是改這裡的一個數字，不用去程式碼裡找。

**對你的意義**：例如哪天內部規範從 5% 改成 4%，你只要把 `RATIO_WARN: 5` 改成 `4` 就好。

---

## 改動二：修好利率反推（A-1 + A-2）

**做法**：把現行的「猜測值 0.1 起跳、步長每次減半」換成標準二分法，搜尋區間固定為 0% ~ 100%。

**行為變化**：

| 情況 | 現在 | 改完後 |
|---|---|---|
| 真實利率 20% 以下 | 正確 | **完全不變**（實測 77 組差異為 0） |
| 真實利率 20~100% | 一律顯示 20.0000% ❌ | 顯示正確數字 + 跳警告 |
| 真實利率 >100% | 一律顯示 20.0000% ❌ | 顯示 `＞100%` |

**A-2 會自動跟著修好**：現在的警告寫的是 `if (rate > 20)`，而舊演算法最多只回傳「恰好 20.0000」，所以永遠不成立。換成新演算法後 22% 就是 22%，警告自然就會正常跳出來。

**一個必須補的洞**：新演算法在超出範圍時會回傳 `NaN`。程式裡「佣後利率」和「稅佣後利率」都已經有防呆，但**「稅後利率」漏了一個**，不補的話那個欄位會直接顯示「NaN」三個字。這次一併補上。

---

## 改動三：統一金額上限（A-3）

| 欄位 | 現行上限 | 改為 | 說明 |
|---|---|---|---|
| 本金 | 999,999,999 | **99,999,999** | 收緊。破億的案件本來就不該用快速工具算 |
| 期繳 | 999,999 | **99,999,999** | 放寬。解掉「2000 萬 12 期算得出來卻打不進去」 |
| 推廣費 | 999,999 | **99,999,999** | 放寬。防打錯字交給比例警告負責 |
| 期數 | 999 | 999 | 不變 |
| 資金成本 | 20% | 20% | 不變 |

**另外要補的一致性問題**：目前這些上限**只擋數字鍵盤的手動輸入**，用「+100 / +千 / +萬」這類調整鍵、以及「1%~5%」快捷鍵完全不受限制。這次讓四條輸入路徑套用同一套檢查：

- 數字鍵盤輸入
- `adjustPrincipal` / `adjustPayment` / `adjustCommission` 微調鍵
- `setCommissionPercent` 比例快捷鍵
- 從歷史記錄「載入計算」

---

## 改動四：修掉利率 >50% 的靜默夾擠

`js/calc-ui.js` 第 85 行目前是：

```js
const safeRate = Math.min(Math.max(rate, 0.0001), 50);
```

導致「欄位顯示 60%，期繳卻是用 50% 算的」。改成用 `LIMITS.MAX_RATE`（100），並在數字鍵盤輸入超過 100% 時直接擋下來，讓「欄位顯示的」和「實際計算用的」永遠是同一個數字。

---

## 改動五：警告的觸發邏輯

**利率超過 20%**、**佣金比例超過 5%** 兩者採用同一套模式：

- **紅字**：只要目前超標就一直顯示紅色（這部分現在已經有了，不改）
- **提示訊息**：只在「從沒超標變成超標」的那一瞬間跳一次

**為什麼要這樣設計**：`updateAllFields()` 每按一次按鈕就會執行一次。如果寫成「超標就跳提示」，你連按「+千」十下就會連跳十次提示，在客戶面前會很干擾。

**實際體感**：

```
佣金比例 4.8%  →  按 +千  →  5.2%   💬 跳提示一次，欄位轉紅
             →  再按 +千  →  5.6%   （安靜，欄位維持紅色）
             →  再按 +千  →  6.0%   （安靜，欄位維持紅色）
             →  按 -萬    →  4.1%   （安靜，紅色消失）
             →  按 +千    →  5.2%   💬 再跳一次提示
```

**已確認**：按「5%」快捷鍵得到剛好 5.00%，依「不可超過 5%」的規範**不會**觸發警告，只有超過才會。

---

## 改動六：sw.js 版本號（1 行，但很關鍵）

`sw.js` 的 `CACHE_VERSION = 'v1.0.2'` 要往上加。

**為什麼一定要改**：JS 檔案走的是「快取優先」策略。如果不動版本號，你把上面五項全部改完推上去，**使用者手機裡跑的還是舊版程式，利率照樣被鎖在 20%**。這一行不改，前面全白做。

（快取機制本身的問題屬於第二批 C 級，這次先不動它，只把版本號往上加，讓這批修正能確實送達。）

---

## 改完之後你會看到的變化

**不會變的：**

- 所有畫面排版、按鈕位置、顏色 — 完全不動
- 20% 以下的每一個計算結果 — 完全一致
- 支票頁、發票頁、加油頁 — 完全不動
- 已存的歷史記錄 — 完全保留，資料格式不變

**會變的：**

1. 利率超過 20% 的案件，開始顯示真實數字（原本都是 20.0000%）
2. 「利率超過 20%」的警告開始會跳（過去從未跳過）
3. 佣金比例超過 5% 時會跳一次提示（過去只有紅字）
4. 期繳最大可輸入到 99,999,999（原本 999,999）
5. 本金最大從 999,999,999 降為 99,999,999
6. 利率打超過 100% 會被擋下來（原本可以打進去但算錯）

---

## 驗證方式

改完後我會做這幾項確認：

1. **回歸測試**：拿 77 組（期數 12~84 × 利率 2%~19.9%）跑新舊版對照，確認 20% 以下**逐筆數字完全相同**
2. **邊界測試**：本金 99,999,999 / 期繳上限 / 推廣費上限 / 利率 100% 各測一次，確認擋與不擋的界線正確
3. **警告測試**：確認跨過 20% 和 5% 時各跳一次、連按不會重複跳
4. **不回歸測試**：確認支票頁、發票頁、加油頁的檔案「一個字元都沒被改到」（用 git diff 檢查）
5. **公司名稱掃描**：再掃一次全部檔案，確認沒有出現不該出現的公司名

---

## 如果改壞了怎麼辦

這個專案在 git 上，每一次改動都可以完整還原。

- 我會把這批改動獨立成**一次 commit**，不跟其他改動混在一起
- 萬一發現問題，一行指令就能退回到現在的狀態，不會有殘留
- 建議你在我改完後、推上 GitHub 之前，先在電腦上實際點一遍確認

---

## 這批不會處理的（留給後續批次）

- 快取與更新機制的整體整併（C 級）
- 版面 15px 偏移、dvh、safe-area、加油頁 viewport（B 級）
- 主畫面捷徑、manifest 清理（D 級）
- 小鍵盤三份合一、加油頁拆檔（E 級）
- 月曆翻頁 bug、備註特殊符號（F 級）

刻意維持小步走，每批改完都能單獨驗證、單獨退回。

# 修改成果：第二批（C 級 — 更新與快取）

完成日期：2026/07/25
狀態：**已完成並通過驗證，尚未推上 GitHub**

---

## 一句話總結

「我明明改了，手機上卻沒變」的三個原因都修好了，而且以後版本號會自動遞增、你不用再記得改。

---

## 改了哪些檔案

| 檔案 | 改動 |
|---|---|
| `sw.js` | 快取清單 13 → 23 項、逐一預快取、油價網路優先、移除強制接管 |
| `index.html` | 新增更新提示條，移除自動重整與詢問框 |
| `pages/gas.html` | 油價抓取拿掉時間戳（1 處） |
| `js/common-modals.js` | 頁尾改為顯示真實版本號 |
| `scripts/bump-version.py` | **新增** — 版本號遞增腳本 |
| `.github/workflows/version-bump.yml` | **新增** — 推送後自動執行 |

**沒有動到**：`js/calc-*.js`、`js/check-engine.js`、`js/invoice-engine.js`、`css/`、計算／支票／發票三個頁面、說明頁、關於頁。已用檔案指紋逐一確認。

（`manifest.json` 這次沒動 —— 它的版本號會由 Action 在第一次推送時自動更新。）

---

## 實際修好了什麼

### 1. 版本號自動遞增（C-1）

版本號設為 **4.24**，之後每次推送自動 +0.01。

```
4.24 → 4.25 → 4.26 → ... → 4.99 → 5.00 → 5.01
```

**用整數運算處理**，避免小數相加產生 `4.250000000000001` 這種誤差。實測 4.98→4.99→5.00→5.01、9.99→10.00 全部正確。

**你完全不用碰版本號。** 推上 GitHub 後，Action 會自動改寫 `sw.js` 和 `manifest.json` 並提交。

**只有這些檔案變動才會遞增**：`js/`、`css/`、`pages/`、`instructions/`、`about/`、`icons/`、`index.html`、`404.html`、`sw.js`
**不會遞增的**：`data/`（油價）、`*.md`（說明文件）

### 2. 「確實更新」的三層保障

**第一層**：版本號自動變 —— 不會再忘記。

**第二層**：安裝時強制抓最新檔案。加上了「不要用暫存」的標註，繞過 GitHub Pages 那個「10 分鐘內可用舊版」的指示。實測確認 23 個檔案全部都用這個模式抓取。

**第三層**：頁尾顯示真實版本號。改為**直接向 Service Worker 詢問**「你現在跑的是哪一版」，不是寫死的字串。

```
重車貸款業務工具箱 • v4.25
```

**你唯一要做的**：打開 App 看一眼頁尾，跟 GitHub 上的數字比對。一樣就是最新版。

### 3. 離線快取補齊（C-2）

從 13 項補到 **23 項**，補上原本漏掉的：

- `js/check-engine.js`、`js/invoice-engine.js`、`js/common-modals.js` ← 沒這三個，**離線時支票頁和發票頁根本開不起來**
- 四個工具說明頁
- 隱私政策、服務條款

### 4. 順手修掉一個沒列進報告的問題

原本的預快取是「一次抓 13 個檔案，任何一個失敗就整批失敗」，而且**失敗還被程式吞掉不報錯** —— 等於你的離線功能可能早就是壞的，而你完全不會知道。

改成逐一抓取。實測：故意讓 1 個檔案失敗，其餘 22 個仍成功快取，並明確記錄是哪一個失敗。（舊版在同樣情況下會變成 0 個。）

### 5. 油價快取不再撐爆（C-3）

**實測結果**：連續抓取 50 次後，快取仍**只有 1 筆**。舊版在同樣情況下會變成 50 筆並持續累積。

離線時會用上次抓到的油價，功能不中斷（已實測）。

### 6. 更新機制整併（C-4）

**舊版三套打架**：
1. Service Worker 安裝完就強制接管
2. 偵測到接管 → 自動整頁重整
3. 同時又跳「發現新版本，是否立即更新？」

導致「按了取消結果還是重整了」，且有機率在你跟客戶輸入到一半時發生。

**現在只有一套流程**：

```
偵測到新版本
  → 畫面最底部滑出一條小提示：「已有新版本 v4.25　[立即更新]  ✕」
  → 不擋住任何操作，不影響輸入

  不理它／按 ✕  → 下次完全關掉 App 再開，自動生效
  點「立即更新」→ 立刻套用並重新載入
```

**最關鍵的安全設計**：整個網站現在**只剩一處**會呼叫「重新載入」，而且必須先經過一個「使用者主動要求」的旗標。實測確認：沒點「立即更新」的情況下，即使 Service Worker 換手，也絕不重整。

提示條有做 iPhone 底部 Home 指示條的安全距離，不會被蓋住。

**注意**：「完全關掉 App」是指**從多工清單把它滑掉**，不是按 Home 鍵切到背景。

---

## 驗證結果

| # | 項目 | 結果 |
|---|---|---|
| 1 | 安裝行為（23 檔全快取、reload 模式、不強制接管） | 8 / 8 通過 |
| 2 | 單一檔案失敗不拖垮全部 | 2 / 2 通過 |
| 3 | 啟用時清理舊版快取 | 通過 |
| 4 | 油價抓 50 次仍只有 1 筆快取 | 2 / 2 通過 |
| 5 | 離線時油價功能不中斷 | 通過 |
| 6 | 一般資源快取優先（開啟速度） | 通過 |
| 7 | 版本號查詢 + 異常訊息防呆 | 3 / 3 通過 |
| 8 | 強制接管只能由使用者點擊觸發 | 2 / 2 通過 |
| 9 | 更新提示條三種情境（不理它／點更新／首次安裝） | 14 / 14 通過 |
| 10 | 版本號進位（含 4.99→5.00、9.99→10.00） | 通過 |
| 11 | 遞增腳本不會破壞 sw.js／manifest.json 其他內容 | 通過 |
| 12 | 不該動到的 17 個檔案未被更動 | 通過 |
| 13 | 第一批的計算修正仍正常（274 筆回歸零差異） | 通過 |
| 14 | 全部 JS／Python／YAML 語法檢查 | 通過 |

合計 **34 項自動化測試全數通過**。

---

## 驗證過程中的一件事，據實說明

測「使用者不理它時不會重整」這一項時，一開始顯示通過 —— 但我檢查後發現那是**空過的**：監聽器根本還沒註冊，所以測試等於什麼都沒測。

修正測試流程（真正觸發頁面載入事件讓監聽器註冊）後重測，才是真的通過。

我特別把這件事寫出來，是因為這一項正是這批改動最重要的保證。如果我沒發現，就會給你一個「已驗證」的假象。

---

## ⚠️ 上線前請務必照這個順序

Service Worker 會常駐在使用者手機裡。**如果推上去一個壞掉的版本，它可能會一直把壞版本餵給使用者**，即使你已經在 GitHub 上修好了。

**第一步：電腦 Chrome 測**（電腦上清除容易，手機很麻煩）
- 開啟網站，按 F12 → Application → Service Workers，確認版本
- 看頁尾有沒有顯示版本號
- 切到四個分頁都點一遍
- F12 → Network 勾選 Offline，重新整理，確認支票頁和發票頁都打得開

**第二步：你自己的手機測**
- 加到主畫面，開起來確認正常
- 確認頁尾版本號

**第三步：都沒問題才通知其他業務同事**

### 萬一手機卡住了

1. **關掉 App 再開兩次** —— 大部分情況這樣就好
2. **移除主畫面圖示，重新加入一次**
3. **手機瀏覽器設定 → 清除該網站資料**

**第 3 點請特別小心：這會把試算歷史記錄一起清掉。** 測試階段請不要找正在跑重要案子的同事當白老鼠。

---

## 有一件事我在電腦上驗不了

Service Worker 的完整更新流程（新版偵測 → 提示條出現 → 點擊套用 → 重新載入）需要真實瀏覽器多次開關才能完整驗證。

我驗證了：安裝邏輯、快取策略、所有重新載入的路徑都必須經過使用者點擊、提示條在三種情境下的行為。但**真實環境的第一次更新**還是要靠你實際測。

具體來說，第一次推上去之後，你會需要**再推一次小改動**（例如改個註解），才能看到「更新提示條」實際出現的樣子 —— 因為要有「舊版在跑」才會偵測到「有新版」。

---

## 目前累積的狀態

| 批次 | 內容 | 狀態 |
|---|---|---|
| 第一批 A 級 | 利率算錯、金額上限、警告機制 | 已完成，未推上線 |
| 第二批 C 級 | 更新與快取 | 已完成，未推上線 |

兩批都還沒 commit。你可以一起測、一起推，或是分開來比較好追問題。

---

## 還沒處理的

- **B 級**：版面上方被蓋住 15px、iOS 底部被切、加油頁縮放與其他頁不一致
- **D 級**：主畫面四個捷徑全開到計算頁、manifest 有壞掉的分享設定
- **E 級**：小鍵盤三份重複、加油頁 1720 行未拆、主題色散在 10 個檔案、200KB 沒用到的函式庫
- **F 級**：月曆選 31 號時翻頁失效、備註打特殊符號會壞掉

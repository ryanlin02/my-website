# 重車貸款業務工具箱 — 全站健檢報告

檢查日期：2026/07/25
檢查範圍：全部 24 個檔案、約 15,500 行程式碼
檢查方式：逐檔閱讀 + 實際執行核心計算函式驗證

---

## 先說結論

網站整體是「能用、而且用得不錯」的狀態。設計理念（緊湊版面、禁止彈出鍵盤、固定標題列快速切換）**執行得相當徹底**，這點做得比大多數個人專案好。

但經過 423 次修改累積下來，有 **1 個會算錯數字的嚴重問題**、幾個手機版面的小瑕疵，以及一堆「不影響現在使用、但會讓你之後每次修改都更痛苦」的結構問題。

以下按嚴重度排列。

---

## 🔴 A 級：會算出錯誤數字（建議優先處理）

### A-1　利率反推被鎖死在 20%，而且不會報錯

**檔案**：`js/calc-engine.js` 的 `RATE()` 函式

這個函式負責「輸入期繳金額，反推利率」。它用的搜尋方式，數學上的搜尋範圍剛好是 **0% ~ 20%**，超過 20% 的真實利率一律回傳 `20.0000%`。

我實際跑過驗證：

| 真實利率 | 程式反推結果 |
|---|---|
| 15% | 14.9993% ✅ |
| 18% | 17.9992% ✅ |
| 20% | 20.0000% ✅ |
| **22%** | **20.0000% ❌** |
| **25%** | **20.0000% ❌** |
| **30%** | **20.0000% ❌** |

**影響範圍**：稅前利率反推、稅後利率、佣後利率、稅佣後利率 — 四個欄位全部經過這個函式。

**為什麼危險**：它不會跳警告、不會顯示「計算錯誤」，就是安靜地給你一個看起來很正常的 20.0000%。如果現場報價時遇到高利率短期案件，你看到的數字是錯的，而且沒有任何跡象。

**修改方式**：改用標準的二分法，把搜尋上限拉到 100%，真的超出範圍時顯示「超出計算範圍」而不是硬給一個數字。

**會造成的改變**：20% 以下的所有計算結果**完全不變**（我已用多組數值驗證過）。只有原本會被錯誤壓成 20% 的案件，會開始顯示正確數字。屬於「只修好、不改變現有行為」的修改。

---

### A-2　「利率超過 20%」的警告永遠不會跳出來

`js/calc-ui.js` 裡有這段：

```
if (rate > 20) showToast('警告：計算結果顯示利率超過20%...');
```

但因為 A-1 的關係，`RATE()` 最大只會回傳「恰好 20.0000」，永遠不會 `> 20`。所以這個警告從寫下去到現在，**一次都沒有觸發過**。

修好 A-1 之後這個警告就會自動恢復正常，不需要另外處理。

---

### A-3　推廣費的上限標準不一致

- 用數字鍵盤手動輸入推廣費：上限 **999,999**（超過會擋下來）
- 用「1% / 2% / 3% / 4% / 5%」快捷按鈕：**沒有上限檢查**

所以本金 3,000 萬按「5%」會直接填入 1,500,000，繞過了 999,999 的限制。兩條路徑標準不同，結果不一致。

**修改方式**：把上限統一（建議提高到 9,999,999，因為重車案件本金本來就大），兩條路徑套用同一套檢查。

---

## 🟠 B 級：手機版面問題（部分違反你當初的設計原則）

### B-1　每個功能頁最上面 15px 被標題列蓋住

`index.html` 裡：標題列高度設 **60px**，但內容區卻從 **45px** 開始。

結果是內容區的最上面 15px 藏在標題列後面，同時底部多出 15px 的空白。這在「版面盡可能緊湊」的前提下是實打實的浪費。

**修改方式**：把兩個數字對齊（改成都是 60px）。
**會造成的改變**：所有功能頁的內容會整體往下移 15px，變成完整可見；底部空白消失。你會覺得畫面「正」了。

---

### B-2　備註編輯框會彈出手機鍵盤 ⚠️ 違反設計原則

**檔案**：`js/calc-storage.js`、`js/check-engine.js`

全站所有輸入框都設成 `readonly` + 自製數字小鍵盤，鍵盤抑制做得非常徹底 — **唯一的例外是歷史記錄的「備註」編輯框**。它是 `<textarea>`，而且程式還主動呼叫了 `.focus()`，等於強制叫出鍵盤。

更麻煩的是，當初為了補救這個問題寫的 `adjustNoteEditorPosition()` 函式，兩個分支的內容**一模一樣**（都是 `paddingTop: 180px`），等於這個補丁完全沒有作用。

**可選的修改方式**：

| 方案 | 說明 | 改動幅度 |
|---|---|---|
| ① 移除 `.focus()` | 至少不強制彈鍵盤，使用者自己點才彈 | 最小，1 行 |
| ② 改成鍵盤彈出時把彈窗頂到上方 | 用 `visualViewport` 正確計算位置，鍵盤不會遮到 | 中等 |
| ③ 做成全螢幕備註頁 | 鍵盤怎麼彈都不會遮擋，最徹底 | 較大 |

備註本來就需要打中文字，完全不彈鍵盤做不到。我建議 ①+② 一起做。

---

### B-3　用 `100vh` 會在 iOS 上被切掉

Safari 的網址列會自動收起／展開，`100vh` 算的是「網址列收起時」的高度。所以在網址列展開的狀態下，畫面**底部會被切掉一截**。

**修改方式**：改用 `100dvh`（現在的手機瀏覽器都支援了）。
**會造成的改變**：底部不再被切。純改善，不影響已安裝成 PWA 的使用情境。

---

### B-4　沒有處理 iPhone 的 Home 指示條

全站找不到任何 `env(safe-area-inset-*)` 設定。iPhone X 以後的機型，螢幕最底部那條白色橫線區域會蓋住你的底部按鈕（例如「清除全部輸入」）。

**修改方式**：底部加上 safe-area 的內距。
**會造成的改變**：iPhone 上底部按鈕不再被蓋；Android 上完全沒變化。

---

### B-5　加油頁的縮放比例跟其他頁不一樣

| 頁面 | viewport 設定 |
|---|---|
| 主頁 / 計算 / 支票 / 發票 | `width=400`（固定寬度） |
| **加油頁** | **`width=device-width`（跟著手機螢幕）** |

意思是：其他頁都假裝螢幕是 400px 寬然後等比縮放，加油頁卻用手機的實際寬度。所以從「發票」切到「加油」時，字體大小和間距會有一個微妙的落差 — 你可能一直有感覺但說不上來哪裡怪。

**修改方式**：加油頁改成 `width=400`，跟其他頁一致。
**會造成的改變**：加油頁的字體和間距會變得跟其他頁一樣。**這頁的視覺會有明顯變化**，需要你實際看過確認可以接受。

---

### B-6　有三個 CSS 變數沒定義，樣式靜默失效

| 檔案 | 未定義的變數 | 實際後果 |
|---|---|---|
| `index.html` | `--transition-normal` | 隱私權／服務條款頁的淡入動畫其實沒有在動 |
| `pages/gas.html` | `--text-primary`、`--button-hover` | 這幾處的文字顏色和按鈕滑過效果變成瀏覽器預設 |

CSS 的特性是：變數不存在時整條規則靜靜地失效，不會報錯。所以這種問題可以存在很久沒人發現。

**修改方式**：補上定義（3 行）。改動極小、零風險。

---

## 🟡 C 級：更新與快取（「我明明改了，手機上卻沒變」的原因）

### C-1　Service Worker 版本號從沒更新過

`sw.js` 的版本號停在 `v1.0.2`，而你已經改了 423 次。

JS 和 CSS 走的是「**快取優先**」策略 — 意思是使用者打開 App 時，先給他手機裡存的舊版，同時偷偷在背景下載新版。所以：

> **你今天更新的東西，使用者今天看不到；要關掉 App 再重開一次才會生效。**

如果你曾經遇過「我明明改好了，同事手機上還是舊的」，這就是原因。

**修改方式**：每次改完把版本號往上加。或是加一個自動帶版號的機制。
**會造成的改變**：改動生效變快（下次開啟就是新版）。

---

### C-2　離線快取清單漏了三個檔案

`sw.js` 的預快取清單有計算頁的三個 JS，但**漏掉了**：
`js/check-engine.js`、`js/invoice-engine.js`、`js/common-modals.js`、以及全部的 `instructions/` 和 `about/` 頁面。

結果：如果使用者裝好 App 後、還沒點過支票頁就進到沒訊號的地方（地下停車場、工地），**支票頁和發票頁會開不起來**。

**修改方式**：把漏掉的檔案補進清單。
**會造成的改變**：離線可用性完整。安裝時多下載約 60KB。

---

### C-3　油價查詢會把快取撐爆

`pages/gas.html` 抓油價時網址加了時間戳（`?v=1753...`）來防快取。但 Service Worker 不認得這是同一個檔案，會把**每一次**的請求各存一份。

用久了，手機裡會累積成千上萬筆一模一樣的油價資料。

**修改方式**：把油價這個請求排除在快取之外（1 行設定）。

---

### C-4　三套更新機制互相打架 ⚠️

現在同時存在三個更新流程：

1. `sw.js` 安裝時無條件呼叫 `skipWaiting()`（立刻強制接管）
2. `index.html` 監聽到接管後**自動整頁重新載入**
3. `index.html` 同時又會跳出 `confirm('發現新版本，是否立即更新？')`

三個湊在一起的後果：**有機率在你正在跟客戶輸入數字時，畫面突然整頁重整，或跳出一個更新詢問框**。

雖然有自動存檔機制會救回大部分資料，但在客戶面前發生這種事還是很尷尬。

**修改方式**：只保留一套。建議改成「靜默更新，下次開啟才套用」— 完全不打斷使用者。
**會造成的改變**：更新變得無感。你需要多等一次開關 App 才會看到新版（但不再有突然重整的風險）。

---

## 🟡 D 級：PWA 設定問題

### D-1　主畫面的四個捷徑全部都開到「計算」頁 ❌

`manifest.json` 定義了四個長按圖示的捷徑：

```
計算 → /my-website/?page=calculator
支票 → /my-website/?page=check
發票 → /my-website/?page=invoice
加油 → /my-website/?page=gas
```

但我搜遍全站，**沒有任何一行程式碼讀取 `?page=` 這個參數**。所以四個捷徑點下去，全部都是開到預設的計算頁。

**修改方式**：在 `index.html` 加約 5 行程式，讀取網址參數並切到對應分頁。
**會造成的改變**：長按 App 圖示的四個捷徑會真的有用。這其實是滿實用的功能 — 直接跳到支票頁。

---

### D-2　分享功能是壞的

`manifest.json` 的 `share_target` 指向 `"/"`（github.io 根目錄，不是你的網站），而且用 `POST` 方法 — GitHub Pages 根本不支援 POST。

**修改方式**：直接刪掉這段（你的工具用不到分享接收功能）。

### D-3　manifest 裡有兩個不合法的欄位

`"permissions"` 和 `"features"` 不是 PWA manifest 的合法欄位，瀏覽器會直接忽略。刪掉即可。

### D-4　版本號三個地方對不上

| 位置 | 版本 |
|---|---|
| `manifest.json` | 1.0.1 |
| `sw.js` | v1.0.2 |
| 頁尾（`common-modals.js`） | **v2.5.0** |

建議統一，並且以後只改一個地方。

---

## 🔵 E 級：結構問題（不影響現在使用，但會影響你之後每次修改）

這一區是「300 次修改後為什麼會愈改愈亂」的根本原因。

### E-1　數字小鍵盤有三份一模一樣的程式碼 ⚠️ 最關鍵

`calculatorInput` / `calculatorEquals` / `calculatorOperation` / `calculatorClear` / `calculatorBackspace` / `showToast` / `vibrate` / `closeModal` …

這一整套邏輯在下列三個地方**各存在一份**：

- `js/calc-ui.js`（計算頁）
- `js/check-engine.js`（支票頁）
- `pages/gas.html`（加油頁，寫在 HTML 裡）

**這就是為什麼「改了計算頁的小鍵盤，支票頁沒跟著變」。** 你（或 AI）改了一份，另外兩份沒動，然後不一致就這樣累積下來。

**修改方式**：抽成一個共用的 `js/calculator-pad.js`，三個頁面都引用它。
**會造成的改變**：功能上完全一樣，但以後改一次三頁同步。**這是投報率最高的一項改動** — 它會讓之後所有的修改都變簡單。
**風險**：需要仔細比對三份的差異（可能有刻意的不同），改完要三頁都測過。

---

### E-2　加油頁是唯一沒拆檔的頁面

| 頁面 | 總行數 | 內嵌 CSS | 內嵌 JS |
|---|---|---|---|
| 計算 | 344 | 0 | 65 |
| 支票 | 457 | 291 | 19 |
| 發票 | 657 | 422 | 19 |
| **加油** | **1,720** | **744** | **734** |

計算頁已經拆得很乾淨了（CSS 和 JS 都獨立），加油頁卻是 1,720 行全部塞在一個檔案裡。它是全站最難改的檔案。

**修改方式**：比照計算頁，拆成 `css/gas.css` + `js/gas-engine.js`。
**會造成的改變**：外觀行為完全不變，但檔案從 1,720 行變成 240 行 + 兩個獨立檔案。

---

### E-3　主題配色在 10 個檔案裡各定義一次

`--primary-color: #333333` 這組變數，在 `index.html`、`calculator.css`、`gas.html`、`invoice.html`、4 個說明頁、2 個關於頁 — **共 10 個地方**各寫一份。

想換個配色，要改 10 個地方，漏一個就會有一頁顏色不對。

**修改方式**：抽成一個 `css/theme.css`，全站引用。

### E-4　載入了 200KB 但完全沒用到的函式庫

`pages/calculator.html` 載入了 `html2canvas`（截圖用，約 200KB），但全站**沒有任何一行程式碼用到它**。應該是某次想做「試算結果存成圖片」但沒做完的殘留。

刪掉即可 — 計算頁載入會變快，離線也少佔 200KB。

### E-5　死程式碼

`loanCalculatorEmergencyBackup` 這個備份：程式裡有「讀取」和「刪除」的程式碼，但**從來沒有任何地方寫入過它**。是永遠讀不到東西的死路。

### E-6　`.htaccess` 完全沒有作用

那是 Apache 伺服器的設定檔。GitHub Pages 用的不是 Apache，**這個檔案 100% 被忽略**。裡面寫的快取規則和重導向規則一條都沒生效。

（好消息：Service Worker 已經在做同樣的事了，所以沒有實際損失。）建議刪掉，免得以後誤以為它有在運作。

### E-7　正式環境還在跑 60+ 個除錯訊息

全站有 60 多個 `console.log`。不影響功能，但會拖慢一點點速度，也讓 F12 主控台被洗版、真的出錯時反而看不到。

---

## 🟢 F 級：小 bug

### F-1　月曆的「上一月／下一月」有時候按了不會換月 ✅ 已實測確認

**檔案**：`js/check-engine.js`

如果目前選的日期是 **29、30、31 號**，按「上一月」不會真的退到上個月。

實測：
```
起始日期：2026/5/31
按「上一月」→ 2026/5/1   ❌（還在五月！）
再按一次   → 2026/4/1   （這時才到四月）
```

原因是程式直接對日期做 `setMonth(-1)`，遇到「4 月 31 日」這種不存在的日期時，JavaScript 會自動往後溢位。

（附帶一提：支票排程本身的月底處理是**正確**的，有先把日期歸到 1 號再算。只有月曆的翻頁箭頭沒做這個處理。）

**修改方式**：翻頁時先把日期設為 1 號再換月（2 行）。

---

### F-2　備註打特殊符號會弄壞歷史記錄

歷史記錄的備註內容被直接組進 HTML 和按鈕的 onclick 字串裡，只處理了單引號 `'`。

所以備註如果打了 **雙引號 `"`**、**換行**、或 **`<` `>`** 這類符號，那一筆記錄的「載入計算」和「刪除」按鈕會失效。

**修改方式**：改用安全的方式綁定事件、並跳脫特殊字元。

### F-3　計算頁有兩列殘留的隱藏欄位

`pages/calculator.html` 裡的「佣後利率」和「稅佣後利率」兩列，網頁載入後被 JS 立刻隱藏（因為改到頂部固定列顯示了）。HTML 還留著但永遠看不到。可以清掉。

### F-4　折扣金額按鈕的標示容易誤會

加油頁的折扣按鈕標示是 `-1 / 4 / 5 / 6 / 7 / 8 / 9 / +1`，但實際值是 `0.1 / 0.4 / 0.5 ...` 元。

單位（角）沒有標出來，剛上手的人容易以為按「5」是折 5 元。

---

## ✅ 檢查後確認沒問題的部分

這些我特別驗證過，做得是對的：

- **國字大寫轉換** — 發票頁和支票頁是兩套完全獨立寫的實作。我拿 24 組金額對照跑過（包含 `1000001`→壹佰萬零壹、`100200`→壹拾萬零貳佰、`100000000`→壹億 等最容易出錯的值），**兩邊結果一致且全部正確**。
- **支票排程的月底處理** — 31 號遇到 2 月的情況有正確處理，沒踩到月份溢位。
- **稅金計算** — 用「總利息 ÷ 21」反推 5% 營業稅，數學正確。
- **PMT 期繳計算** — 標準等額本息公式，正確。
- **鍵盤抑制** — 全站輸入框都是 `readonly` + 自製小鍵盤，執行得非常徹底（唯一例外是 B-2 的備註框）。
- **自動存檔** — 換頁、離開、切到背景都有存檔，資料保護做得不錯。
- **油價自動更新** — GitHub Actions 每週一早上 5 點自動抓，設定正確，還有抓取失敗時的靜態備援值。

---

## 建議的修改順序

| 階段 | 內容 | 風險 | 效益 |
|---|---|---|---|
| **第一批** | A-1 利率上限、A-3 推廣費上限、F-1 月曆翻頁、B-1 版面 15px、B-6 CSS 變數 | 低 | 直接修掉會算錯／看得到的問題 |
| **第二批** | C-1~C-4 快取與更新、D-1~D-4 PWA 設定 | 低 | 改動能即時生效、捷徑能用、不再突然重整 |
| **第三批** | B-2 備註鍵盤、B-3 dvh、B-4 safe-area、B-5 加油頁 viewport | 中（外觀會變） | 手機體驗一致化 |
| **第四批** | E-1 小鍵盤合併、E-2 加油頁拆檔、E-3 主題抽出、E-4~E-7 清理 | 中 | **讓之後每一次修改都變簡單** |

第一批和第二批可以放心做，改完你不會覺得「怪怪的」。
第三批會改變外觀，建議做完之後你實際在手機上看過再決定要不要保留。
第四批是整理，功能完全不變，但需要比較仔細的測試。
